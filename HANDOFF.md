# 引き継ぎ

最終更新: 2026-08-29

## いま何をしているのか

**在庫の素材から 9/2・9/3 分（長尺2本＋ショート2本）を作り終えた。まだアップロードしていない。**

予約投稿は 9/1 まで埋まっている。今日やったのは
**アナリティクスの再確認（まだ測れない）** と **2日分の新規制作**。

**クォータの都合でアップロードは JST 16:00 以降**（回復時刻）。
今日はまだ1本も上げていないが、8/28 の 16:00 以降に6本上げているので
**16:00 までは残量がほぼ無いとみて動く**。

## 今回やったこと

| やったこと | 結果 |
|---|---|
| ワークツリーを main に fast-forward | **それ以前は初期コミット1個だけだった**（前回と同じ罠） |
| `report_stats.py --traffic` | データ最終日 **2026-08-26**。施策日 8/27 にまだ届かない |
| `sync_playlists.py`（ドライラン） | **足す0本・0ユニット**。8/29分はまだ private |
| レシピ `2026-08-29-horie-subsc` を新規作成 | 素材 `w_3GLEVLQCQ` の未使用区間から |
| レシピ `2026-08-29-kawai-jitai` を新規作成 | 素材 `PqPxW3wYR7M` の未使用区間から |
| 長尺2本・ショート2本をビルド、サムネを目視 | 6点とも通した（horie のサムネは2回作り直し） |

## 検証済みの事実（実際に画面に出した出力）

### 作ったもの（未アップロード）

| ファイル | 尺 | 中身 |
|---|---|---|
| `work/2026-08-29-horie-subsc/video.mp4` | **1069.000000s（17:49）** | 長尺。論点カード35枚 |
| `work/2026-08-29-horie-subsc-short/video.mp4` | **56.966666s** 1080x1920 | ショート。字幕21枚 |
| `work/2026-08-29-kawai-jitai/video.mp4` | **911.666666s（15:12）** | 長尺。論点カード36枚 |
| `work/2026-08-29-kawai-jitai-short/video.mp4` | **50.281000s** 1080x1920 | ショート。字幕16枚 |

尺はすべて ffprobe で読んだ値。サムネ `thumb.png` も2本とも生成済み。

| レシピ | 素材 | 長尺の区間 | ショートの区間 | 既存との重なり |
|---|---|---|---|---|
| `horie-subsc` | `w_3GLEVLQCQ` 堀江 城光・キャンドル | 441.0–1510.0 | 1428.039–1485.0 | 既存 `horie-candle`（1505.24–）の直前まで。無し |
| `kawai-jitai` | `PqPxW3wYR7M` 河合 直人・ロンドン | 61.84–973.5 | 260.079–310.36 | 既存 `kawai-eigyou`（1909–）よりずっと手前。無し |

### 焼き込みテロップで裏取りしたもの

`ffmpeg -ss <秒> -i work/<ID>/source.mp4 -frames:v 1` で抜いて目で読んだ。

| 元動画 | 出た文字 | 秒 |
|---|---|---|
| `w_3GLEVLQCQ` | 「日本発のキャンドルを日常へ／堀江 城光（35歳）」 | 443 ほか全編 |
| `w_3GLEVLQCQ` | 「300万円 投資」 | 110 |
| `w_3GLEVLQCQ` | 「キャンドルをサブスク」／「絶対無理」 | 443 / 446 |
| `w_3GLEVLQCQ` | 「年商7,000万円」 | 875 |
| `w_3GLEVLQCQ` | 「めちゃくちゃ幼稚」 | 1483 |
| `w_3GLEVLQCQ` | 「竹之内 教博／ユダヤ式教育の伝道師」 | 1405 |
| `w_3GLEVLQCQ` | 「えぴろ／株式会社 Epi Group 代表」 | 941 |
| `PqPxW3wYR7M` | 「海外旅行をもっと安心・身近にしたい／河合直人（43歳）」 | 293 ほか全編 |
| `PqPxW3wYR7M` | 「100万円 投資」 | 100 |
| `PqPxW3wYR7M` | 「希望額100万円／残り100万円」 | 295 |
| `PqPxW3wYR7M` | 「ふざけないでほしい」 | 293 |
| `PqPxW3wYR7M` | 「本家・令和の虎で完全ALLを辞退／志願者が語るその理由とは?」 | 679 |
| `PqPxW3wYR7M` | 「谷本 吉紹（47）／株式会社エースタイルホールディングス 代表」 | 295 |
| `PqPxW3wYR7M` | 「南 忠則／株式会社WOODBASE・F 代表」 | 745 |

判定は2本とも既存レシピから引き写した（`horie-candle` の完全ALL成立、
`kawai-eigyou`／`kawai-rehab` の完全ALL 100万円）。
**どちらの区間もまだ判定前**なので `verdict.detail` にその旨を書いてある。

### ビルドの確認

- **`horie-subsc` のショートで巻き戻しを1回踏んで直した。** `start=1428.0` だと
  `1428.0 → 1423.6` まで戻り、竹之内のプロフィール挿入ナレーション
  「が容赦なく本質を突きつける」が頭に入った。
  subs.json の cue 開始 **1428.039** ちょうどに直して解消（ドライランで警告消滅）。
  `kawai-jitai` は最初から cue 境界（260.079）にしたので巻き戻し無し
- **ビルド後のショート2本からフレームを抜いて確認した。** どちらも本編から始まり、
  番組の挿入カードは入っていない。head テロップも出ている
- **`fixes` が長尺区間の字幕に当たることをコードで数えた。**
  `horie-subsc` 16件・`kawai-jitai` 23件、**どちらも全部 HIT**。
  ショート区間では一部しか当たらない（ドライランの「当たらなかった」警告は正常）
- **`horie-subsc` のサムネは2回作り直した。** `crop_bottom` 0.26/0.30 だと元テロップの
  上端が残ったので **両方 0.32** に。0.35 は顔が寄りすぎたので戻した。
  `kawai-jitai` は 0.30/0.32 で一発で通った
- サムネ2枚とも 頭切れ・人物取り違え・吹き出しの被りは無し
- 長尺のフレームを確認：`horie-subsc` は冒頭のブリーフカードと中盤の論点カード、
  `kawai-jitai` は冒頭のブリーフカード（志願者行が2行に収まっている）
- **`kawai-jitai` は最初のドライランで「案件カードの志願者が2行に収まらない」と
  弾かれた。** `profile` を短くして通した。**この警告は本ビルドを中断させる**
- **論点カードの文言をビルド後の映像で1枚検算したら、誤りが1件出た。**
  ASR は「その行動が正義なのかっていうのを取りたいんですよ」だが、
  焼き込みテロップは **「正義なのか問いたい」**。カードを「問いたいんですよ」に直し、
  `fixes` にも足して**長尺を作り直した**。
  **ASRの動詞は名詞と同じくらい崩れる。カードに引用を書くときは映像で確認する**

### アナリティクス

**まだ施策日（8/27）に届いていない。** データ最終日は **2026-08-26**（前回は 08-25）。
`cardImpressions` も `RELATED_VIDEO` も `YT_PLAYLIST` も 0 のままだが、**評価にならない**。

- 新しく **`SHORTS_CONTENT_LINKS` が 6再生**（前回は無かった）
- トラフィック: `SHORTS 32,630` `YT_SEARCH 534` `YT_OTHER_PAGE 334` `YT_CHANNEL 161`
- 配信が始まる前の公開分 18本 / 合計再生 159（1本あたり 8.8）

## 次にやること

### 1. 8/29 の公開分を再生リストに入れ、カードを1枚入れる（12:10 以降）

**予約公開の動画は private なので `sync_playlists.py` は入れない。公開日にやる。**

| 日 | 長尺 | 入れる再生リスト | ティーザー |
|---|---|---|---|
| 8/29 | `B9iCtgqAZaY` | 不成立 | 不成立になった回まとめ |
| 8/30 | `-_kvvlKpvfA` | 不成立 | 不成立になった回まとめ |
| 8/31 | `VgVtuZMdFQk` | 完全ALL成立回 | 完全ALLで決まった回まとめ |
| 9/1 | `v4jVPOkfPkU` | 協力確約 | 協力確約で終わった回まとめ |
| 9/2 | （下のアップロード後に決まる） | 完全ALL成立回 | 完全ALLで決まった回まとめ |

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/sync_playlists.py
python scripts/sync_playlists.py --apply
```

カードは Studio で1本ずつ（手順は下の「触ってはいけないところ」）。

### 2. 9/2・9/3 分をアップロードする（JST 16:00 以降）

**書き込む系は本体から実行する。** work だけワークツリー側を絶対パスで渡す。
**順番は長尺→ショート**（ショートの概要欄が長尺のURLを台帳から引くため）。
**4本で 1,600×4＋サムネ50×4＝6,600ユニット。**

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
WT=C:/Users/oshim/Documents/projects/tora-kirinuki/.claude/worktrees/long-short-video-creation-bddbd4
python scripts/upload_youtube.py "$WT/work/2026-08-29-horie-subsc"       --schedule "2026-09-02T03:10:00Z"
python scripts/upload_youtube.py "$WT/work/2026-08-29-horie-subsc-short" --schedule "2026-09-02T03:00:00Z"
python scripts/upload_youtube.py "$WT/work/2026-08-29-kawai-jitai"       --schedule "2026-09-03T03:10:00Z"
python scripts/upload_youtube.py "$WT/work/2026-08-29-kawai-jitai-short" --schedule "2026-09-03T03:00:00Z"
```

公開後に入れる再生リストは **どちらも「完全ALL成立回」**
（ティーザーは「完全ALLで決まった回まとめ」）。

### 3. 残りの素材から作る

未使用の検出済み候補が **4件**。**再ダウンロードは要らない。**
**アップロードは1日6本まで**（1,600×6＋サムネ50×6＝9,900ユニット）。
**クォータの回復は JST 16:00。**

| 元動画 | 区間 | score | 下見の結果 |
|---|---|---|---|
| `huV8XPdmcc4` 坂口 | 0:00:00〜0:15:05 | 149.0 | **字幕を読んだ。山場が薄い**（下記） |
| `xfr6w7g_Xuc` 駒澤 | 0:26:17〜0:41:15 | 115.2 | 未確認 |
| `DSCYmCBAp_I` Maiちゃん | 0:12:34〜0:27:39 | 86.7 | 未確認 |
| `TuYYB2N0JGE` 齋藤 | 0:12:11〜0:27:08 | 78.9 | 未確認 |

`VrgC54_ESTk`・`bah_FU172NA`・`w_3GLEVLQCQ`・`PqPxW3wYR7M` は使い切った。
（`w_3GLEVLQCQ` は 0–441 の経歴パートだけ残るが、ホームレスの話は
8/10 公開分で既に使っている。`PqPxW3wYR7M` は 973–1909 が残るが、
新プランの説明パートで山場が弱い）

**`huV8XPdmcc4` 坂口の未使用部分（0–1650）は説明とAR体験が中心で山場が薄い。**
虎が切り込むのは 1650 以降で、そこは既存 `2026-08-19-sakaguchi-cvc` が使っている。
加盟店の社長が「入ってるのに使えてない」と正直に言う場面（1000〜1150）と
虎がARグラスを体験する場面（1200〜1650）はあるので、作るならそこ。

**score は機械的な点数。中身に山場があるかは1本ずつ確かめること。**

### 4. 8/30 以降にアナリティクスを測り直す

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/report_stats.py --traffic
```

出力の先頭の「データが入っている最終日」が **2026-08-27 以降**になっていること。
見るのは `RELATED_VIDEO` / `YT_PLAYLIST` / `cardImpressions` /
`SHORTS_CONTENT_LINKS`（今日 6）／ Studio の「関連のショート動画」。

## 触ってはいけないところ・保留中の判断

**ワークツリーは main から分岐していることがある。** 今回も前回も、
作業前に `git reset --hard main` が要った。**最初に `git log --oneline -3` を見ること。**

**レシピを作る手順**

1. `work/<元動画ID>/subs.json` を `scripts/show_subs.py <id> <開始> <終了> --every 40` で
   流し読みし、山場のある連続区間を選ぶ（13〜20分）
2. **タイトルの引用は必ず焼き込みテロップで裏取りする。**
   `ffmpeg -ss <秒> -i work/<ID>/source.mp4 -frames:v 1 out.jpg` で抜いて目で読む
3. 金額・氏名も同じ方法で裏取り。**ASRの数字と固有名詞は信用しない**
4. 同じ元動画の既存レシピがあれば、判定・氏名・希望額はそこから引き写せる
5. `--dry-run` を長尺・ショート両方で通してから本ビルド
6. **サムネ PNG を目視。** 元テロップが残ったら `crop_bottom` を上げて `--thumb-only`
7. **ショートは必ずビルド後の動画からフレームを抜いて確認する**

**ショートの巻き戻しが番組の挿入カードを巻き込む。今回も踏んだ（2回目）。**
`build_short` は開始点を文頭まで戻す。**`short.start` は subs.json の
cue の開始時刻ちょうど**にすること（例: 1428.0 ではなく 1428.039）。
ドライランの「巻き戻し」の行と、ビルド後の実映像の両方を見る。

**`fixes` は順番に単純置換される。短い方が後だと二重に当たる。**
「牛タ」→「牛タン」が既存の「牛タン」にも当たって「牛タンン」になった過去がある。
広い置換のあとに戻しを置く。**当たるかどうかは事前に数えられる**：

```python
txt=''.join(c['line'] for c in cues if s<=c['t']<=e)
for k in fixes: print('HIT' if k in txt else 'MISS', k, txt.count(k))
```

**長尺には自前の字幕を焼かない**（元動画の焼き込みテロップをそのまま使い、
上に論点カードを重ねる）。`fixes` が効くのはショートの字幕。
**ショートの `quote` はコード側で意図的に焼いていない**（話者の取り違えを避けるため）。

**カードを入れる手順（過去に2回、カードを2枚作ってしまった）**

```
studio.youtube.com/video/<ID>/edit
→ 右カラムを下にスクロールして「カード」の鉛筆を目で見つけて押す（位置は動画ごとに違う）
→ 「再生リスト」行の【＋】を押す（1回で開かないことがある。開かなければもう一度＋を押す。
   この時点ではカードは作られていないので安全）
→ ★選択ダイアログが開いたのをスクリーンショットで確認してからタイルを1回押す★
→ ★もう一度スクリーンショットを撮り、ダイアログが閉じたことを確認する★
→ 時刻を 0:45 に（書式は尺で変わる。4:32なら "0:45:00"、15:04なら "00:45:00"。
   入力後に【Tab】を押さないと確定しない）
→ ティーザーを入れる → 右上「保存」→ グレーになったのを確認
```

**★を飛ばすとカードが2枚できる。** 2枚目は真ん中のタイル（協力確約）の誤選択。
行のゴミ箱で消す。**タイル順は 完全ALL / 協力確約 / 不成立。**

**Studio に入るときは必ず
`studio.youtube.com/channel/UCWupHXqf8CTG00c_eoQSw1Q` から。**
既定チャンネルは別チャンネル。上部の「12.7K/16/4,012」は vidIQ の数字で登録者数ではない。

**書き込む系（upload / sync_playlists --apply）は本体から実行する。**
`upload_youtube.py` は `__file__` で台帳の場所を決めるので、ワークツリーから
実行するとワークツリー側の `state/published.json` に書かれて分岐する。

**Bash ツール経由で Python の heredoc を書くときの落とし穴**
- stdin の既定エンコーディングが cp932。**先頭に `# -*- coding: utf-8 -*-` が要る**
- **`\n` はツールを通る間に実改行になる。** 文字列に改行を入れたいときは `chr(10)`
- Python の二重引用符の文字列に `"` を入れると構文エラー。`「」『』` を使う
- **長い heredoc を2つ繋げると bash が構文エラーを出す。**
  長文ファイルは Write ツールで書く
- **出力が化けるときは `python -c` の先頭で `sys.stdout.reconfigure(encoding='utf-8')`**

**`sync_playlists.py` は非公開の動画を入れない。** 予約公開分は公開日に流し直す。
`mai-teian` は判定が分類に当たらないので触らない。**推測で分類しない。**

**関連動画は今も0件のまま。** 原因不明（POST は 200、`no_match_illustration` が返る）。

**重複アップロード `yHxbK4Y05zI`（`NSAi-BfomiU` と同内容）が残っている。
削除は取り返しがつかないので相談してから。**

**8/26公開の `Atsm_3CKLx8` は焼き込み字幕が訂正前のまま。** 差し替えは取り下げになる。

**音を聞かないと確定しないASRの崩れが残っている。推測で `fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |
| `higaki-shizumu` | 「報道した方がいいのかな」（僭越ながらの直前） |
| `akutsu-jisshoku` | 「焼き鳥3級」の正式名、「ジムハイボール」 |
| `hayama-kyoudou` | 羽山さんの会社名（「株式会社山の」としか出ない） |
| `horie-subsc`（新規） | 「成果もいけるんですけれども」（花瓶の話。生花か）、「気宝ホイッピング法」、「乱車というタイトル」、「コツぶ型」、「エベスさん／恵ベさん」（えべっさんか） |
| `kawai-jitai`（新規） | 「引きの市の政病院」、「都営し」（渡英か）、「歩行除」、「霊オ見てるんですよね」、「リューピ」「森明さん」（人名）、「めっちゃ許った」（気を遣ったか）、「ウォールアーナシング」、「た野さん／竹野さん」（谷本か別人か不明） |

## 未検証のもの

- **予約した6本（8/30・8/31・9/1）が予定時刻に公開されるかは当日まで分からない。**
  台帳と API の応答では `publishAt` が入っている
- **8/29 12:00/12:10 の公開もまだ起きていない**（この文を書いた時点で 10:00 前）
- **新規の長尺2本（17:49 と 15:12）は通しで見ていない。** 見たのはビルドログ、
  サムネPNG、ショートの実映像、長尺の数フレームだけ
- **論点カードの文言を焼き込みテロップと突き合わせたのは1枚だけ。**
  その1枚で誤りが1件見つかった（下記）。**残りの70枚は突き合わせていない**
- 8/27・8/28 に入れたものが数字を動かすかは不明。**8/30 以降に測る**
- **カード10本のうち再読み込みで確認したのは3本。** 残り7本は保存がグレーに
  なったことまで
- **`SHORTS_CONTENT_LINKS` の6再生が何を指すかは未確認**（Studio の
  「関連のショート動画」に当たるかどうか）
- **前回の「インプレッション 2,387」の出所は不明**
- **終了画面が入っている長尺の全数は数えていない**（分かっているのは3本）
- 長尺の残存カーブ（`elapsedVideoTimeRatio`）は未測定
- 08-20 の配信断絶の原因は不明のまま
- **`xfr6w7g_Xuc` / `DSCYmCBAp_I` / `TuYYB2N0JGE` の未使用区間は中身を見ていない**
