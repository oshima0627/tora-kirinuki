# 引き継ぎ

最終更新: 2026-09-10（JST 17時台）

## いま何をしているのか

**アナリティクスを測り直して、5つの施策を実装した日。** 予約投稿は 9/12 まで
埋まっている（9/11 `iwata-personal`、9/12 `iwaya-tanpen`）。**9/13 以降は未着手。**

**★方針が2つ変わった（大島さんの判断）。**

1. **ショートの尺を 65〜73秒 → 50〜58秒にした。** これは **TikTok Creator Rewards の
   「1分以上」を外れる。** アカウントが未作成（`state/tiktok.json` は空）なので、
   実測で効いている YouTube の平均視聴率を取った。
2. **長尺を毎日作るのをやめた。** 1日ショート6本に切り替える。
   `build_clip.py` は消していないので、作ると決めた回だけ動かせる。

**★次にやることは「尺だけ変えた5本」を作って答え合わせすること。**
**論点カード（実装済み）は、この5本では使わないこと。** 同時に入れると
どちらが効いたか分からなくなる。

## 今回やったこと（2026-09-10）

| やったこと | 結果 |
|---|---|
| ワークツリーを main に reset | **また初期コミット1個だけだった**（**7回連続**） |
| アナリティクスを n=19 で測り直した | `docs/2026-09-10-analytics.md` |
| **尺の窓を 50〜58秒にした** | `recipe.py`。45秒未満で落ちる。窓の外は警告 |
| **区間探索をスクリプトにした** | `scripts/find_short.py`（HANDOFF の手書きスニペットは廃止） |
| **論点カードをショートに入れられるようにした** | `short.points`。**既定では入らない** |
| **トレーラーを差し替えた** | `VwO47R4gGsE`（2再生）→ `0V9WKZLW2Dw`（85.7%）。**適用済み** |
| **固定コメントの投稿を実装した** | `scripts/post_question.py`。**まだ投稿していない** |
| 長尺の毎日制作をやめた | `daily-workflow.md` を書き直した |
| **既存バグを1件見つけて直した** | ショート2本目（`-short2`）に長尺リンクが入らなかった |
| テスト | **215 → 267 passed** |

## ★測って分かったこと（n=19・Analytics の実出力）

詳細は `docs/2026-09-10-analytics.md`。ここには判断に使うものだけ置く。

| | 値 |
|---|---|
| **実視聴秒** | **中央値 39.5秒。尺との相関 -0.02（＝無関係）** |
| 尺 × 平均視聴率 | **-0.53** |
| 65秒以下 n=7 / 68秒以上 n=6 | 平均視聴率 中央値 **66.7%** / **54.3%** |
| 3秒残存 × 平均視聴率 | +0.86（前回 n=12 で +0.90。**保った**） |
| 開始位置 × 平均視聴率 | -0.69（前回 -0.73。**保った**） |
| 15〜25分帯 n=8 / 帯外 n=11 | **68.2%** / 57.5% |
| 登録者 | **10人**（およそ1万再生で1人） |
| 共有 / コメント | **0** / 7 |
| 長尺（8/20以降22本） | 外れ値2本を除くと20本で91再生＝**1本4.6** |

**3秒の残存は全19本が113%以上。フックはもう直すところが無い。**
**落ちるのは7〜10秒地点**（上位88〜98% / 下位74〜76%）。

## 次にやること

### 1. ★尺だけ変えた5本を作る（最優先）

**論点カードは使わないこと。** 尺の効果だけを測る。

```bash
python scripts/find_short.py <video_id> --from 15 --to 25 --letterbox
python scripts/build_short.py recipes/<id>.json --dry-run
```

`find_short.py` は「文頭に着地して巻き戻しが0秒」の開始点だけを出す。
`--letterbox` は元動画の黒帯を輝度で測る（**2026-09-07 に踏んだ罠**。
`2TYcrm--MqQ` の 1380〜1406秒で検出できることを実測で確認済み）。

**答え合わせ**: 5本の平均視聴率の中央値を **既存19本の中央値 60.9%** と比べる。
60秒以下で作った既存7本の中央値は 66.7%。**70%を超えれば効いたと言える。**
**再生数では見ない。**

### 2. そのあと、論点カードで5本

`recipes/<id>.json` の `short.points` に書く。

```json
"points": [{"at": 7.0, "sec": 4.0, "text": "本部も店も利益を取る。両方が取れるほど儲かるのか"}]
```

`at` はショートの先頭からの秒。**1枚目は7〜10秒**（外れると警告が出る）。
上帯（見出しの場所）がカードに切り替わる。映像と字幕は触らない。

**答え合わせ**: 7秒・10秒地点の残存を、既存の中央値（7秒 99% / 10秒 89%）と比べる。

### 3. 固定コメントを投げる（★文面の承認待ち）

**まだ1件も投稿していない。** 文面は私が書いた下書きなので、読んでから流すこと。

下書きを入れてあるレシピ:

| レシピ | question |
|---|---|
| `2026-09-04-higaki-toshokan` | 虎は「シェア図書館は違う」と言いました。あなたが虎なら出資しますか? |
| `2026-09-07-iwata-personal` | 虎は「ビッグ3専門店にしろ」と代案を出しました。あなたが虎なら出資しますか? |

```bash
python scripts/post_question.py recipes/<id>.json           # 出すだけ
python scripts/post_question.py recipes/<id>.json --apply
```

**★ピン留めは YouTube Data API v3 に無い**（2026-09-10 に discovery を実測。
`commentThreads` は insert/list のみ、`comments` に pin は無い）。
**投稿したあと Studio で手でピン留めすること。**

**予約公開中（private）の動画はコメント欄が無効として返る**（実測）。
公開された日に流すこと。

### 4. 公開日ごとに再生リスト

**予約公開の動画は private なので `sync_playlists.py` は入れない。公開日にやる。**

| 日 | 長尺 | 入れる再生リスト | 済 |
|---|---|---|---|
| 9/5〜9/9 | `JOBx3jI5Xvw` `teRlKkX00c4` `BIB8bQxXzj4` `CpjcMM1RMXk` `HllE-g7dgXU` | 各判定 | 未 |
| 9/10 | `jsaJeI_9dXs` | 協力確約 | 未 |
| 9/11 | `xHL66uDgLPc` | 不成立 | 未 |
| 9/12 | `_1Ah8QyAKTI` | 完全ALL成立回 | 未 |

### 5. TikTok

**アカウント作成は大島さんの作業。** 設定内容は
[`docs/2026-08-31-tiktok-account-setup.md`](docs/2026-08-31-tiktok-account-setup.md)。
**★始めるときは、同じ区間の `end` を伸ばした65秒以上の別ビルドにすること。**
`validate_short` の下限は45秒なので、50〜58秒版と65秒版を両方作れる。

## 検証済みの事実（実際に画面に出した出力）

- **テスト 267 passed**（215 → 267。追加したのは `test_find_short` 11 /
  `test_short_points` 20 / `test_short_chain` 6 / `test_question` 8 / `test_funnel` 7）
- **`find_short.py` を実素材で通した。** `2TYcrm--MqQ` の 15〜25分で59件。
  9/10 分に使った開始点 `963.079` に対して 52.68 / 54.04 / 56.24秒 を出した
  （実際に作ったのは 70.9秒）
- **黒帯の検出を既知の区間で確かめた。** `2TYcrm--MqQ` の 1385/1395/1405秒 は検出、
  963/990/1019秒 は検出なし
- **論点カード入りのショートを実際にビルドして、8秒・20秒のフレームを目で見た。**
  上帯がカードに切り替わり、映像と下帯の字幕はそのまま。カード終了後は見出しに戻る
- **トレーラーの差し替えは読み直して確認した**（`unsubscribedTrailer = 0V9WKZLW2Dw`）
- **ピン留めが API に無いことは discovery を読んで確認した**（推測ではない）
- **既存31レシピが全部読める**（全部が窓の外の警告を出すが、エラーは0件）

## 未検証のもの

- **9/11・9/12 の予約が予定どおり公開されるかは当日まで分からない**
- **50〜58秒のショートを1本も公開していない。** 効果は未測定
- **論点カードを入れたショートを1本も公開していない。** 効果は未測定
- **固定コメントを1件も投稿していない**
- **7秒地点で何が起きているかは映像を見て確かめていない。**
  「7秒に2つ目の情報を出す」は仮説
- **新しい作り方（9/7〜）の答え合わせはまだできない。** Analytics のデータは 9/7 まで。
  使える標本は 9/8 の1本（53.3%）だけで、しかも74.6秒＝過去最長。
  **9/14〜15 に `report_stats.py --retention` で測り直すこと**

```bash
python scripts/report_stats.py --retention
```

## 触ってはいけないところ・保留中の判断

**ワークツリーは main から分岐していることがある。今回で7回連続、
作業前に `git reset --hard main` が要った。最初に `git log --oneline -3` を見ること。**

**書き込む系（upload / sync_playlists --apply / post_question --apply /
set_trailer --apply）は本体から実行する。** `upload_youtube.py` は `__file__` で
台帳の場所を決める。**`work/` はジャンクションで共有だが `scripts/` は共有されない。**

**アップロードは1日6本まで**（1,600×6＋サムネ50×6＝9,900）。**回復は JST 16:00。**
**`--schedule` は未アップロードの動画に直接付けること**（先に上げてから入れ直すと
`videos.update` の50が1本ごとに余分にかかり、6本で10,200になって超過する）。

**★`channels.update` の直後に読み直すと古い値が返る**（2026-09-10 実測）。
update の応答も更新前の値を返すので、応答は検証に使えない。数秒おいて引き直すこと
（`set_trailer.py` はそうしてある）。

**★Analytics は 500 backendError を散発的に返す。** `set_trailer.py` に
リトライを入れてある。他のスクリプトで踏んだら同じ形で足すこと。

**レシピを作る手順**

1. `show_subs.py <id> <開始> <終了> --every 120` で流し読みして山場を探す
2. **既存レシピと台帳で未使用区間を確認する**
3. **`find_short.py <id> --from 15 --to 25 --letterbox` で候補を出す**
4. **タイトル・金額・固有名詞は必ず焼き込みテロップで裏取りする**
   （`ffmpeg -ss <秒> -i work/<ID>/source.mp4 -frames:v 1 out.jpg` で抜いて目で読む）
5. `--dry-run` を通す
6. 本ビルド → **サムネPNGを目視** → 被りが出たら `bias` を0.02刻みで下げて `--thumb-only`
7. **ショートは必ずビルド後の動画からフレームを抜いて確認する**

**★サムネの被りは目視で判定しない。** `build_clip.py` が毎回自動で検査する。
`bias` を 0.02刻みで下げ、0になった最初の値を採る。**`x` は動かさない。**

**★元テロップが残ったら、下なら `crop_bottom`、上なら `crop_top` を上げる。**

**★カードを根拠に虎の名前を書かないこと。** 挿入プロフィールカードは
直後の発言者と一致しないことがある。

**`fixes` は順番に単純置換される。短い方が後だと二重に当たる。**
**キューをまたぐ文字列は一致しない。**

**長尺には自前の字幕を焼かない。`fixes` が効くのはショートの字幕だけ。**

**Bash ツール経由で Python の heredoc を書くときの落とし穴**
- stdin の既定エンコーディングが cp932。**先頭に `# -*- coding: utf-8 -*-` が要る**
- **`\n` はツールを通る間に実改行になる。** 改行は `chr(10)`
- **出力が化けるときは `sys.stdout.reconfigure(encoding='utf-8')`**
- **長文ファイルは Write ツールで書く**

**関連動画は今も0件のまま。** 原因不明（POST は 200、`no_match_illustration` が返る）。

**重複アップロード `yHxbK4Y05zI`（`NSAi-BfomiU` と同内容）が残っている。
削除は取り返しがつかないので相談してから。**

**8/26公開の `Atsm_3CKLx8` は焼き込み字幕が訂正前のまま。** 差し替えは取り下げになる。

**在庫の素材**: `HowwrqNF9gw`（VEALL・関西版80人目・71.3分）はまるごと手つかず。
**★扱いに注意**（「売名やろ？」と福祉を掲げる3人組を追及する回。そのまま切ると
名誉を害する編集になりやすい）。駒澤 `xfr6w7g_Xuc` の 23.0〜34.5分も使える
（**34分以降は41歳未婚・恋愛遍歴の追及なので必ず外すこと**）。
山本・岩田・岩谷の3本も未使用区間が残る。

**音を聞かないと確定しないASRの崩れが残っている。推測で `fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `yamamoto-caremane` | 「かに取るか」「毛山マネ」「FC拾隊」 |
| `iwata-personal` | 「エアロビクスの要請」「妖怪ご高い方」「イコマシ」「1万2以上」 |
| `iwaya-tanpen` | 「女監督」（**ショート区間内**）「盛坂」「執年映画」「脚音」 |
