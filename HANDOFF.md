# 引き継ぎ

最終更新: 2026-08-31（JST 14時台）

## いま何をしているのか

**予約投稿は 9/3 まで埋まった。9/4 分の長尺・ショートも作り終えたが、まだ上げていない。**

今日やったのは **8/29 公開分の再生リスト追加とカード挿入**、
**9/2・9/3 分のアップロード（4本）**、**9/4 分の新規制作**、
**アナリティクスの再確認（まだ測れない）**。

**8/31 に 9/4 分をアップロードした。予約は 9/4 まで埋まった。**
今日のクォータ枠（8/30 16:00〜8/31 16:00）の消費は 3,300（アップロード2本）
＋100（再生リスト2本）＝ **3,400**。残り約 6,600。

**TikTok は投稿できていない。** 方針は「照会を待たずに投稿する」に変えたが、
**専用アカウントがまだ無い**（下記）。

**8/30 に TikTok 配信を設計して実装まで終えた。**
設計 [`…-tiktok-distribution-design.md`](docs/superpowers/specs/2026-08-30-tiktok-distribution-design.md) /
計画 [`…-tiktok-distribution.md`](docs/superpowers/plans/2026-08-30-tiktok-distribution.md)。
**main に取り込み済み**（マージコミット `e7144f2`、push 済み）。

## 今回やったこと

| やったこと | 結果 |
|---|---|
| ワークツリーを main に fast-forward | **それ以前は初期コミット1個だけだった**（3回連続で同じ罠） |
| `sync_playlists.py --apply` | 8/29公開分 **2本を「不成立」に追加**（100ユニット） |
| Studio で `B9iCtgqAZaY` にカードを1枚挿入 | 00:45:00・不成立・ティーザー入り。**再読み込みで確認済み** |
| `upload_youtube.py` ×4（長尺→ショートの順） | 4本とも成功。サムネも4本とも設定済み |
| `report_stats.py --traffic` | データ最終日 **2026-08-26 のまま**（朝と同じ）。施策日 8/27 に未到達 |
| レシピ `2026-08-29-mai-namesugi` を新規作成、ビルド | 長尺15:35・ショート70.1s。サムネは1回作り直し |
| TikTok配信の設計・計画・実装（8/30） | 5タスク。テスト **176 → 186 passed**。main 取り込み済み |
| 9/4分をアップロード（8/31） | 長尺 `4w0QdsxKAa4` / ショート `1AYT85SM7Eg`。サムネも設定済み |
| 8/30公開分を再生リストへ追加（8/31） | `-_kvvlKpvfA` / `5cvw_zqL52g` を「不成立」に。**1日遅れだった** |
| 8/31公開分を再生リストへ追加（8/31） | `VgVtuZMdFQk` / `0V9WKZLW2Dw` を「成立」に |
| レシピ `2026-08-31-saito-shikaku` を新規作成 → 9/5枠でアップロード | `JOBx3jI5Xvw` / `0Qn-fWy4-kc` |
| **素材の棚卸しをやり直した** | 引き継ぎの「使い切った」が複数まちがい。**7本に13分以上の塊が残っていた** |
| レシピ `2026-08-31-yotsui-nothing` を新規作成、ビルド | 長尺13:44・ショート69.5s。**未アップロード** |
| 8/30・8/31 の長尺にカードを挿入 | 各1枚・`00:45:00`。**2本とも再読み込みで確認済み** |
| TikTok投稿を試みて中止（8/31） | **投稿先が別人のアカウントだった**（下記） |

## 検証済みの事実（実際に画面に出した出力）

### アップロードした4本（予約済み・private）

| レシピ | 動画ID | 公開予定 |
|---|---|---|
| `2026-08-29-horie-subsc`（長尺） | `9CU507QLfC4` | 2026-09-02T03:10:00Z（JST 9/2 12:10） |
| `2026-08-29-horie-subsc-short` | `gwcCP5Y0Hkc` | 2026-09-02T03:00:00Z |
| `2026-08-29-kawai-jitai`（長尺） | `8uCHM2y9h34` | 2026-09-03T03:10:00Z |
| `2026-08-29-kawai-jitai-short` | `T0AyRaoH0co` | 2026-09-03T03:00:00Z |

台帳で4本とも `thumbnail_set = True` / `privacy_status = private` / `publish_at` 入りを確認した。

### 8/29 のカード挿入

`studio.youtube.com/video/B9iCtgqAZaY/edit` → カード →
再生リスト「不成立・虎が詰めた回」1枚のみ、**00:45:00**、
ティーザー「不成立になった回まとめ」。**保存後にページを再読み込みして再確認した。**

**★タイル順が引き継ぎの記載と変わっていた。** 今回のダイアログは
**不成立 / 完全ALL成立回 / 協力確約** の順（以前は 完全ALL / 協力確約 / 不成立）。
**順番を覚えず、毎回スクリーンショットでタイルの文字を読んでから押すこと。**

### 作ったもの（未アップロード）

| ファイル | 尺 | 中身 |
|---|---|---|
| `work/2026-08-31-yotsui-nothing/video.mp4` | **823.800000s（13:44）** 1920x1080 | 長尺。論点カード35枚 |
| `work/2026-08-31-yotsui-nothing-short/video.mp4` | **69.533333s** 1080x1920 | ショート。字幕33枚 |

（`mai-namesugi` は 9/4 枠、`saito-shikaku` は 9/5 枠でアップロード済み）

尺は ffprobe で読んだ値。`thumb.png` も生成済み。

| レシピ | 素材 | 長尺の区間 | ショートの区間 | 既存との重なり |
|---|---|---|---|---|
| `yotsui-nothing` | `VrgC54_ESTk` 四ツ井 裕明・給食（SNS版） | 2155.04–2978.839 | 2444.48–2514.0 | 既存 `yotsui-kyushoku`（–2131）の直後。**無し** |

**この回は判定（ナッシング）まで入っている。** SNS版は全員一致が条件で、
カイシャイン氏だけがごめんなさいだったため支援獲得はならなかった。

### 焼き込みテロップで裏取りしたもの

`ffmpeg -ss <秒> -i work/DSCYmCBAp_I/source.mp4 -frames:v 1` で抜いて目で読んだ。

| 出た文字 | 秒 |
|---|---|
| 「祖母の家を通じて昭和の暖かさを未来に繋げたい／大阪のオカンMaiちゃん（41歳）」 | 520 |
| 「旅館が完成したら沼島に住む?」 | 520 |
| 「釣り人の聖地・沼島で宿泊業に挑む／不透明な事業プランに虎たちも荒ぶる」 | 全編の上部帯 |
| 「希望額880万円／残り880万円」 | 1504・1551 |
| 「不便を楽しんでほしい」 | 1240 |
| 「ターゲットがブレブレ」 | 1091 付近 |
| 「これが半年間の最高傑作」 | 1509 |
| 「舐めすぎでしょ」 | 1521 |
| 「山本 雅俊／株式会社やまもとくん 代表」 | 900 |
| 「中村 英俊／株式会社中村建設 一般社団法人GCU代表」 | 1150 |
| 「南 忠則／株式会社WOODBASE・F 代表」 | 1466 |

**島名は沼島。ASR は全編「主島」と書く**（`fixes` で置換、長尺区間で10箇所）。

**★挿入プロフィールカードは直後の発言者と一致しない。**
1461–1486 に「南 忠則」のカードが入るが、その直後に
「舐めすぎでしょ」と言うのは**眼鏡・パーマの別人**（カードの南さんはハチマキ・無眼鏡）。
**カードを根拠に虎の名前を書かないこと。** 今回は論点カードで虎を名指ししていない
（例外は 谷本。志願者が 1125 で「谷本さん」と直接呼んでいるので音声から特定できる）。

判定・氏名・希望額は同じ元動画の既存レシピ `mai-nushima` から引き写した
（ナッシング 200万円 / 希望額 880万円）。**この区間はまだ判定前**なので
`verdict.detail` にその旨を書いてある。

### ビルドの確認

- **ショートの巻き戻しを踏んだが、原因が分かった。** `short.start` をキュー開始
  ちょうどにしても、**そのキューが接続詞（「だから」など）で始まると
  `continues_previous` で飛ばされ、さらに前まで戻る**（`scripts/moments.py:218` 付近）。
  1506.039 を指定 → 1503.279 まで戻った。**開始点は「接続詞で始まらないキュー」を選ぶ**
- **8/30 に 43.0s から 70.1s へ切り直した。** TikTok配信の設計（下記）で尺の窓を
  65〜73秒に決めたため。開始 1489.279 は巻き戻し無しで着地する（確認済み）。
  裏取りできない「全未完成」は区間に戻ったが、頭ではなく14秒地点なので許容した
- **`fixes` 6件が全部ショート区間で HIT することをコードで数えてから焼いた。**
  最終区間（1489.279–）では6件とも当たっている（ドライランに「当たらなかった」警告が出ない）
- **ビルド後のショートからフレームを抜いて確認した。** 本編から始まり、番組の挿入カードは
  入っていない。head テロップ「半年かけた／事業計画」も出ている
- **論点カードを2枚、ビルド後の映像で焼き込みテロップと突き合わせた。**
  「ターゲットがブレブレ」「舐めすぎでしょ」とも**一致。誤りは出なかった**
- **`thumb` と `tags` を書き忘れて1回ビルドした。** `thumb` が無いと
  サムネが案件カードのままになる。**新規レシピを書くときは既存レシピの
  トップレベルのキーを見比べること**（`id/source_video_id/source_url/source_title/
  clip/cards/short/title/description/tags/category_id/expected_channel_id/
  privacy_status/thumb/fixes`）
- **サムネの左の虎が 1521 だと見切れた**ので 1509（正面向き）に差し替えて通した。
  頭切れ・人物取り違え・吹き出しの被りは無し
- 最初のビルドで「論点カード30が2行に収まらない」で**中断された**。文言を詰めて通した
- 論点カードは最初62枚書いたが、既存が 25秒に1枚なので **41枚（22.8秒に1枚）** に絞った

### TikTok 配信の実装（main 取り込み済み）

| コミット | 内容 |
|---|---|
| `f627648` | `SHORT_MIN_SEC = 65.0` を新設してエラー。既存テスト3本のフィクスチャも直した |
| `71f40a5` | `SHORT_RECOMMENDED_SEC` を 75 → 73 |
| `12c4765` | `build_caption()` と `_source_lines()`（元動画URLを概要欄と共通化） |
| `7c3124c` | `build_short.py` が `caption.txt` を出す |
| `781498f` | `state/tiktok.json`・照会文の草案・`daily-workflow.md` 追記 |

- **テスト 176 → 186 passed。** 統合しようとしているツリーそのもので流した
- **実ビルドで `caption.txt` の中身を目で読んだ。** 1行目にショートのタイトル、
  `【元動画】…` と `https://www.youtube.com/watch?v=DSCYmCBAp_I`、`CREDIT`、末尾にハッシュタグ
- **既存の出力は変わっていない。** `mai-namesugi-short` の尺は着手前と同じ **70.133333**
- **下限65秒を入れた時点で既存テスト3本が巻き添えで落ちた**（`with_short()` の既定が
  50秒だったため）。フィクスチャを 2300.0–2368.0（68秒）に直して解消
- **ビルド検証を最初に本体側から実行してしまい、編集前のコードが動いて
  `caption.txt` が出なかった。** ワークツリー側から流し直して確認した。
  **コードを直したら、直したツリーから実行すること**（`work/` はジャンクションで共有だが
  `scripts/` は共有されない）
- **マージ後の本体でも確認した。** `186 passed`、`SHORT_MIN_SEC=65.0` /
  `SHORT_RECOMMENDED_SEC=73.0`、60秒は
  「short が 60.0秒。TikTok の収益化対象は1分以上なので 65秒を下限にしている」で落ち、
  70秒は通る。実ビルドの尺も `70.133333` のままで `caption.txt` も出た

### アナリティクス

**朝の確認から何も動いていない。** データ最終日は **2026-08-26**（朝も 08-26）。
施策日 8/27 にまだ届かないので、**評価にならない**。

- トラフィック: `SHORTS 32,630` `YT_SEARCH 534` `YT_OTHER_PAGE 334` `YT_CHANNEL 161`
  `SHORTS_CONTENT_LINKS 6` — **すべて朝と同じ数字**
- 配信が始まる前の公開分 18本 / 合計再生 159（1本あたり 8.8）
- 実時間の再生数（Analytics ではない）を見ると、**長尺は 0〜8、ショートは 3,000〜6,000**
  という差が続いている。8/29 分は長尺 `B9iCtgqAZaY` 0 / ショート `Yl3wV9uQnxU` 1,163

## 次にやること

### 1. 9/6 分をアップロードする（8/31 の JST 16:00 以降）

**8/31 の枠はもう 6,900 使った**（アップロード2組6,600＋再生リスト300）。
残り約3,100で1組（3,300）に届かないので、**16:00 の回復を待つ**。

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/upload_youtube.py work/2026-08-31-yotsui-nothing       --schedule "2026-09-06T03:10:00Z"
python scripts/upload_youtube.py work/2026-08-31-yotsui-nothing-short --schedule "2026-09-06T03:00:00Z"
```

**書き込む系は本体から実行する。順番は長尺→ショート。**
公開後に入れる再生リストは **「不成立」**（ティーザーは「不成立になった回まとめ」）。

### 2. 公開日ごとに再生リストとカード

**予約公開の動画は private なので `sync_playlists.py` は入れない。公開日にやる。**

| 日 | 長尺 | 入れる再生リスト | ティーザー | カード |
|---|---|---|---|---|
| 8/29 | `B9iCtgqAZaY` | 不成立 | 不成立になった回まとめ | **済** |
| 8/30 | `-_kvvlKpvfA` | 不成立 | 不成立になった回まとめ | **済**（8/31に実施） |
| 8/31 | `VgVtuZMdFQk` | 完全ALL成立回 | 完全ALLで決まった回まとめ | **済**（8/31に実施） |
| 9/1 | `v4jVPOkfPkU` | 協力確約 | 協力確約で終わった回まとめ | 未 |
| 9/2 | `9CU507QLfC4` | 完全ALL成立回 | 完全ALLで決まった回まとめ | 未 |
| 9/3 | `8uCHM2y9h34` | 完全ALL成立回 | 完全ALLで決まった回まとめ | 未 |
| 9/4 | `4w0QdsxKAa4` | 不成立 | 不成立になった回まとめ | 未 |

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/sync_playlists.py
python scripts/sync_playlists.py --apply
```

カードは Studio で1本ずつ（手順は下の「触ってはいけないところ」）。

### 3. 残りの素材から作る

**★引き継ぎの「未使用区間」の記載が間違っていた（8/31に判明）。**
`recipes/` にある `2026-08-09-test` と `2026-08-10-example` は
**`state/published.json` に無く未公開**。これらを「使用済み」と数えていた。
**未使用区間を出すときは、レシピの有無ではなく台帳にあるかどうかで判定する**：

```python
import json,glob,os
pub=json.load(open('state/published.json',encoding='utf-8'))['videos']
for p in sorted(glob.glob('recipes/*.json')):
    r=json.load(open(p,encoding='utf-8'))
    print(os.path.basename(p), r['source_video_id'], r['clip']['start'], r['clip']['end'],
          '公開/予約済み' if r['id'] in pub else '台帳に無い→未使用扱い')
```

未使用の検出済み候補が **2件**。**再ダウンロードは要らない。**
**アップロードは1日6本まで**（1,600×6＋サムネ50×6＝9,900ユニット）。
**クォータの回復は JST 16:00。**

| 元動画 | 13分以上取れる塊 | 下見の結果 |
|---|---|---|
| `xtJI21E2-xE` 日垣 | 0–1239（20.6分） | **事業説明が中心で山場が薄い**（8/31に字幕を読んだ） |
| `xfr6w7g_Xuc` 駒澤 | 1378–2490（18.5分） | **山場はあるが判断が要る**（下記） |
| `huV8XPdmcc4` 坂口 | 0–1650（27.5分） | **山場が薄い**（下記） |
| `PqPxW3wYR7M` 河合 | 974–1909（15.6分） | 新プランの説明パートで山場が弱い |
| `w_3GLEVLQCQ` 堀江 | 1889–2802（15.2分） | **8/31に詰め語彙を数えたら0件。薄い** |
| `DSCYmCBAp_I` Mai | 0–809（13.5分） | 経歴と宿の説明。山場が無い |
| `TuYYB2N0JGE` 齋藤 | 塊なし（0–1164 と 2345–2632） | 0–1164 は事業説明で薄い |
| `VrgC54_ESTk` 四ツ井 | **8/31に使った**（`yotsui-nothing`） | — |
| `79U5ErgT_Js` 阿久津 / `bah_FU172NA` 今村 / `vi5HC54UaQM` 羽山 | 塊なし | 細切れしか残っていない |

**次に作るなら駒澤。** ただし恋愛パートの判断が要る（下記）。

**score は機械的な点数。中身に山場があるかは1本ずつ確かめること。**

### 4. TikTok — 専用アカウントを作るところで止まっている

実装は main に取り込み済み（`e7144f2`）。マージ後のテストも 186 passed。
**方針は 8/31 に「照会の返事を待たずに投稿する」へ変更した**（大島さんの判断）。

**★投稿できていない理由: 切り抜き用の TikTok アカウントが無い。**
8/31 にブラウザで確認したところ、ログイン中だったのは
**「しま」（ID `30362060737`・フォロワー19・AI画像5本）という無関係のアカウント**だった。
ここへ投稿するとガイドライン4項「チャネル名に『切り抜き』の文言を明記すること」に反し、
キャプションの `CREDIT`（申請済みの切り抜きチャンネルです）も虚偽になるので**中止した**。

**アカウント作成は大島さんの作業。** 設定内容は
[`docs/2026-08-31-tiktok-account-setup.md`](docs/2026-08-31-tiktok-account-setup.md) に用意済み
（表示名「図解でわかる令和の虎【切り抜き】」／自己紹介文／プロフィールのリンク）。

ハンドルが決まったら、`work/2026-08-29-mai-namesugi-short/video.mp4`（70.1秒）と
`caption.txt` をそのまま投稿し、`state/tiktok.json` に記録する。

**権利者へ照会するメールは未送信のまま**（投稿を止める理由ではなくなったが、価値は変わらない）

草案: [`docs/2026-08-30-tiktok-inquiry-draft.md`](docs/2026-08-30-tiktok-inquiry-draft.md)
宛先 `get-clip@razil.jp`。**返事が来るまで TikTok には1本も投稿しない。**
返事に日数がかかる可能性があるので、マージの判断とは別に先に送ってよい。

### 5. 8/30 以降にアナリティクスを測り直す

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/report_stats.py --traffic
```

出力の「データが入っている最終日」が **2026-08-27 以降**になっていること。
見るのは `RELATED_VIDEO` / `YT_PLAYLIST` / `cardImpressions` /
`SHORTS_CONTENT_LINKS`（今 6）／ Studio の「関連のショート動画」。

## 触ってはいけないところ・保留中の判断

**ワークツリーは main から分岐していることがある。** 今回で3回連続、
作業前に `git reset --hard main` が要った。**最初に `git log --oneline -3` を見ること。**

**書き込む系（upload / sync_playlists --apply）は本体から実行する。**
`upload_youtube.py` は `__file__` で台帳の場所を決めるので、ワークツリーから
実行するとワークツリー側の `state/published.json` に書かれて分岐する。
`work/` はジャンクションで共有なので、本体から本体の相対パスを渡せばよい。

**レシピを作る手順**

1. `scripts/show_subs.py <id> <開始> <終了> --every 40` で流し読みし、
   山場のある連続区間を選ぶ（13〜20分）
2. **既存レシピの `clip.start`/`clip.end` を全部出して重なりを確認する**（下のコード）
3. **タイトルの引用は必ず焼き込みテロップで裏取りする。**
   `ffmpeg -ss <秒> -i work/<ID>/source.mp4 -frames:v 1 out.jpg` で抜いて目で読む
4. 金額・氏名も同じ方法で裏取り。**ASRの数字と固有名詞は信用しない**
5. 同じ元動画の既存レシピがあれば、判定・氏名・希望額はそこから引き写せる
6. **既存レシピとトップレベルのキーを見比べる**（`thumb` と `tags` を落としやすい）
7. `--dry-run` を長尺・ショート両方で通してから本ビルド
8. **サムネ PNG を目視。** 元テロップが残ったら `crop_bottom` を上げて `--thumb-only`。
   人物が見切れたら別の秒数のフレームに差し替える
9. **ショートは必ずビルド後の動画からフレームを抜いて確認する**
10. **論点カードを最低2枚、ビルド後の映像で焼き込みテロップと突き合わせる**

```python
import json,glob,os
for p in sorted(glob.glob('recipes/*.json')):
    r=json.load(open(p,encoding='utf-8'))
    print(os.path.basename(p), r['source_video_id'], r['clip']['start'], r['clip']['end'])
```

**論点カードは 22〜25秒に1枚。** 935秒で41枚、911秒で36枚が実績。
**1枚が2行に収まらないと本ビルドが中断する**（実測で49文字は通り、それ以上は落ちた）。

**ショートの巻き戻しの正体（今回判明）。** `short.start` はキュー開始ちょうどにしても、
**そのキューが接続詞で始まると飛ばされてさらに前まで戻る。**
接続詞で始まらないキューを選ぶこと。ドライランは
「開始が発話の途中だったので X → Y まで巻き戻した」と必ず出すので、**それを読む。**

**`fixes` は順番に単純置換される。短い方が後だと二重に当たる。**
「牛タ」→「牛タン」が既存の「牛タン」にも当たって「牛タンン」になった過去がある。
広い置換のあとに戻しを置く。**`fixes` はキューの1行ごとに当たるので、
キューをまたぐ文字列は一致しない**（例:「9.7」と「万円って」が別キューなら
「9.7万円」では当たらない。「万円ってそこまで」で書く）。当たるかどうかは事前に数えられる：

```python
lines=[c['line'] for c in cues if s<=c['t']<=e]
for k in fixes: print(sum(l.count(k) for l in lines), k)
```

**長尺には自前の字幕を焼かない**（元動画の焼き込みテロップをそのまま使い、
上に論点カードを重ねる）。`fixes` が効くのはショートの字幕。
**ショートの `quote` はコード側で意図的に焼いていない**（話者の取り違えを避けるため）。

**カードを入れる手順（過去に2回、カードを2枚作ってしまった）**

```
studio.youtube.com/video/<ID>/edit
→ 右カラムを下にスクロールして「カード」の鉛筆を目で見つけて押す（位置は動画ごとに違う）
→ 「再生リスト」行の【＋】を押す（1回で開かないことがある。開かなければもう一度＋を押す。
   この時点ではカードは作られていないので安全）
→ ★選択ダイアログが開いたのをスクリーンショットで確認する★
→ ★タイルの文字を読んで目的の再生リストを特定してから1回押す★
   **★タイル順は毎回変わる。同じ日の2回の操作の間でも入れ替わった。**
   2026-08-31 の1回目は 不成立 / 完全ALL成立回 / 協力確約、
   **2回目は 完全ALL成立回 / 不成立 / 協力確約**だった。
   本数（不成立12本・完全ALL22本・協力確約6本）も手がかりになる
→ ★もう一度スクリーンショットを撮り、ダイアログが閉じてカードが1枚だけなのを確認する★
→ 時刻を 0:45 に（書式は尺で変わる。4:32なら "0:45:00"、14:54なら "00:45:00"。
   入力後に【Tab】を押さないと確定しない）
→ ティーザーを入れる → 右上「保存」→ グレーになったのを確認
→ ★ページを再読み込みしてカードを開き直し、1枚だけ入っているのを確認する★
```

**★ブラウザが別の Google アカウントにログインしていることがある。**
2026-08-31 にこれを踏んだ。Studio が「このページを表示する権限がありません」を出したら、
右上のアイコン →「アカウントを切り替える」→ **`orfevre6.27@gmail.com`** の下にある
**「図解でわかる令和の虎【令和の虎Second 切り抜き】」**（登録者7人）を選ぶ。

同じ Chrome には `lofilofilofi6.27@gmail.com`（Lofi Tokyo Nights）と
`oshima6.27@gmail.com`（なお／星屑ソラ／RE:GEAR ほか）もサインインしている。
**動画IDを直リンクしても、権限が無いと「エラーが発生しました」としか出ない**ので、
先にアカウントを確かめること。

**登録者数は 7人**（2026-08-31 時点、アカウント切替画面で確認）。
上部の「309/48/6,239」は vidIQ の数字で登録者数ではない。

**Studio に入るときは必ず
`studio.youtube.com/channel/UCWupHXqf8CTG00c_eoQSw1Q` から。**
既定チャンネルは別チャンネル。上部の「12.7K/23/4,110」は vidIQ の数字で登録者数ではない。

**Bash ツール経由で Python の heredoc を書くときの落とし穴**
- stdin の既定エンコーディングが cp932。**先頭に `# -*- coding: utf-8 -*-` が要る**
- **`\n` はツールを通る間に実改行になる。** 文字列に改行を入れたいときは `chr(10)`
- Python の二重引用符の文字列に `"` を入れると構文エラー。`「」『』` を使う
- **長い heredoc を2つ繋げると bash が構文エラーを出す。**
  長文ファイルは Write ツールで書く
- **出力が化けるときは `python -c` の先頭で `sys.stdout.reconfigure(encoding='utf-8')`**
- **`build_clip.py` の警告は日本語が化けて出ることがある。** 化けたら
  `--dry-run` を単独で流し直して読む

**`sync_playlists.py` は非公開の動画を入れない。** 予約公開分は公開日に流し直す。
`mai-teian` は判定が分類に当たらないので触らない。**推測で分類しない。**

**関連動画は今も0件のまま。** 原因不明（POST は 200、`no_match_illustration` が返る）。

**重複アップロード `yHxbK4Y05zI`（`NSAi-BfomiU` と同内容）が残っている。
削除は取り返しがつかないので相談してから。**

**8/26公開の `Atsm_3CKLx8` は焼き込み字幕が訂正前のまま。** 差し替えは取り下げになる。

**音を聞かないと確定しないASRの崩れが残っている。推測で `fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |
| `higaki-shizumu` | 「報道した方がいいのかな」（僭越ながらの直前） |
| `akutsu-jisshoku` | 「焼き鳥3級」の正式名、「ジムハイボール」 |
| `hayama-kyoudou` | 羽山さんの会社名（「株式会社山の」としか出ない） |
| `horie-subsc` | 「成果もいけるんですけれども」（花瓶の話。生花か）、「気宝ホイッピング法」、「乱車というタイトル」、「コツぶ型」、「エベスさん／恵ベさん」（えべっさんか） |
| `kawai-jitai` | 「引きの市の政病院」、「都営し」（渡英か）、「歩行除」、「霊オ見てるんですよね」、「リューピ」「森明さん」（人名）、「めっちゃ許った」（気を遣ったか）、「ウォールアーナシング」、「た野さん／竹野さん」（谷本か別人か不明） |
| `mai-namesugi`（新規） | 「全未完成」（区間外に出した）、「エモい」前後の「VCネっぽい」、「案こたるのカ」（こんなもんかか）、「いつもの着替えが必要」（意気込みか）、「1/3ぐらいは予約埋まってる」の前後（区間外） |

## 未検証のもの

- **予約した8本（8/30〜9/3）が予定時刻に公開されるかは当日まで分からない。**
  台帳と API の応答では `publishAt` が入っている
- **9/4 分の長尺（15:35）は通しで見ていない。** 見たのはビルドログ、サムネPNG、
  ショートの実映像、長尺の3フレームだけ
- **論点カードを焼き込みテロップと突き合わせたのは 41枚中2枚だけ。**
  その2枚は一致した。**残り39枚は突き合わせていない**
- 8/27・8/28 に入れたものが数字を動かすかは不明。**8/30 以降に測る**
- **カード11本のうち再読み込みで確認したのは4本**（今回の `B9iCtgqAZaY` を含む）。
  残り7本は保存がグレーになったことまで
- **`SHORTS_CONTENT_LINKS` の6再生が何を指すかは未確認**（Studio の
  「関連のショート動画」に当たるかどうか）
- **前回の「インプレッション 2,387」の出所は不明**
- **終了画面が入っている長尺の全数は数えていない**（分かっているのは3本）
- 長尺の残存カーブ（`elapsedVideoTimeRatio`）は未測定
- 08-20 の配信断絶の原因は不明のまま
- **`TuYYB2N0JGE` の未使用区間は中身を見ていない**
- **TikTok には1本も投稿していない。** 権利者への照会メールも未送信
- **TikTok 側の挙動は何も測っていない**（伸びるか、収益化条件に届くか）
- **`state/tiktok.json` は空。** 手で書く運用なのでスクリプトは無い
- **`build_caption()` の出力を実際に TikTok の投稿欄に貼っていない**（文字数・改行の見え方は未確認）
