# 引き継ぎ

最終更新: 2026-09-07（JST 10時台後半）

## いま何をしているのか

**予約投稿は 9/12 まで埋まった。** 今日3組を新規に作った。
9/10 は `yamamoto-caremane`（長尺 `jsaJeI_9dXs` / ショート `RdtP-7QPUaQ`）、
9/11 は `iwata-personal`（長尺 `xHL66uDgLPc` / ショート `9tk9hplrqYU`）、
9/12 は `iwaya-tanpen`（長尺 `_1Ah8QyAKTI` / ショート `Ewv59mx4iVU`）。

**★今日のクォータは使い切った（9,900／10,000）。回復は JST 16:00。**

**★新しい作り方が6本目まで来た。5本の閾値を超えたので答え合わせができる。**
1本目 9/7 `sakaguchi-arglass`、2本目 9/8 `kawai-ryoukin`、3本目 9/9 `higaki-toshokan`、
4本目 9/10 `yamamoto-caremane`、5本目 9/11 `iwata-personal`、6本目 9/12 `iwaya-tanpen`。
**9/7 分は公開済みのはずなので、`report_stats.py --retention` で測れる。**
見るのは平均視聴率と3秒地点の残存率。既存12本の中央値 **59.6%** と比べる。**再生数では見ない。**

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/report_stats.py --retention
```

## 今回やったこと（2026-09-07）

| やったこと | 結果 |
|---|---|
| ワークツリーを main に reset | **また初期コミット1個だけだった**（**6回連続**で同じ罠） |
| **字幕が取れない回帰を発見して修正** | `web_safari` が字幕0件。`tv` で引き直すようにした |
| **自動生成字幕が機械翻訳だったのを発見して修正** | `ja` は往復訳。`ja-orig` を優先するようにした |
| **ショート2本目を実装** | `shorts` 配列と `--index`。ドキュメントにあってコードに無かった |
| 新素材を4本取得 | 山本・岩谷・VEALL・岩田。字幕が本物のASRであることを確認済み |
| 堀江・駒澤の未使用区間を自分で読んだ | **堀江は見送り**（決着後）。**駒澤は恋愛パートを外せば使える** |
| レシピ3本を新規作成してビルド・予約投稿 | 下記 |
| テスト | **186 → 215 passed** |

### ★字幕が取れなくなっていた（今日いちばん大きい発見）

**新しい回を4本落とそうとしたら全部「日本語字幕が無い」で中断した。**
取得済みの `xtJI21E2-xE` で同じ経路を試したら `automatic_captions` が **0件**
だったので、**動画側ではなく抽出クライアント側の問題**と分かった。

実測（`xtJI21E2-xE` / yt-dlp 2026.08.19）:

| クライアント | 字幕トラック数 |
|---|---|
| `web_safari`（従来） | **0** |
| `web` / `mweb` | 0 |
| **`tv`** | **157（`ja` / `ja-orig` あり）** |
| `web_embedded` | 157 |

**媒体URLの取得には `web_safari` が要る**（外すと 403）ので、**字幕だけ `tv` で引き直す**
ようにした（`scripts/fetch_source.py` の `CAPTION_CLIENT`）。

### ★さらに、その `tv` の `ja` が機械翻訳だった

`tv` に切り替えたら字幕は取れたが、**読んだら「150万ドル」「タイガース」「それは正しい。」
になっていた。** 日本語ASR → 英語 → 日本語の往復訳を掴んでいた。

`2TYcrm--MqQ` で実測:

| トラック | 行数 | 中身 |
|---|---|---|
| `ja` | 1424 | 「私たちは、利益を生み出す構造を構築するためのノウハウ…」＝往復訳 |
| **`ja-orig`** | **979** | 「利益が出る仕組み構築のノーハウというか、えっと、…」＝**本物のASR** |

**令和の虎は金額が命なので、往復訳を掴むと全カードが壊れる。**
`pick_ja_vtt` を **自動生成は `ja-orig` 優先**に直した（手動字幕は `ja` のまま）。
**既存の取得済み素材は `web_safari` 経由で本物のASRが入っているので影響なし。**

あわせて **`--subs-only`** を足した（`source.mp4` を残して字幕だけ取り直す）。
今回のように字幕だけ壊れたときに1.3GBを落とし直さずに済む。

```bash
python scripts/fetch_source.py https://www.youtube.com/watch?v=<id> --subs-only
```

### ★ショート2本目を実装した（ドキュメントにあってコードに無かった）

`docs/daily-workflow.md` には「`recipes/<id>.json` の `shorts` に2つ目を書きます」と
書いてあったが、**コード側に `shorts` の実装が無く `short`（単数）しか読んでいなかった。**
運用（長尺1・ショート2）がドキュメントどおりに回らない状態だった。

```bash
python scripts/build_short.py recipes/<id>.json               # 1本目 → work/<id>-short
python scripts/build_short.py recipes/<id>.json --index 1     # 2本目 → work/<id>-short2
```

既存レシピは無改造で動く（`higaki-toshokan` の dry-run が変更前と同じ 70.0s・字幕28枚）。

**★今日作った3本のレシピはまだ `short`（単数）1つだけ。** ショート2本目の枠を埋めるには
各レシピに `shorts` を書き足して `--index 1` でビルドする。**クォータの都合で今日は
長尺3本＋ショート3本（6本＝9,900）で打ち止めたため、ショート2本目は未着手。**

### ★ショートの区間に「レターボックス」の罠があった（新発見）

`2TYcrm--MqQ` で最初に選んだ2つの区間が、**どちらも番組の煽り差し込み
（「続く議論 反撃なるか」）に掛かっていた。** この区間は元動画自体が
**上下に黒帯の入ったレターボックス**になっていて、縦型に組むと黒帯が入る。

**目視ではなく端の輝度で測った**（上下8pxの平均輝度が12未満ならレターボックス）:

```python
from PIL import Image; import numpy as np
im=np.asarray(Image.open(f).convert('L'))
print(im[:8,:].mean(), im[-8:,:].mean())   # 両方 <12 ならレターボックス
```

`2TYcrm--MqQ` では **1380〜1406秒**がそれだった。**区間を決めたら、
候補の前後でこれを測ること。**

## 今日作った3本

### 2026-09-07-yamamoto-caremane（9/10）

素材 `2TYcrm--MqQ` 山本悠人（FC版 令和の虎 162人目・50.3分）。ケアプランセンターのFC展開。

| 項目 | 値 |
|---|---|
| 長尺 | 770.80–1615.32（**844.5s / 14:05**）論点カード39枚 |
| ショート | 963.079–1034.0（**70.9s**）＝元動画 **16.05分**（15〜25分帯） |
| 冒頭 | 虎の断定「本部も利益も取ってその店も利益を取った、かなり儲からないとフランチャイズってならないわけよ」 |
| head | 「本部も店も取って／成り立つ?」**巻き戻し0秒** |
| 判定 | **全員ふんわり（協力確約）** |

**焼き込みテロップで裏取りしたもの**

| 出た文字 | 秒 |
|---|---|
| 「分からない」 | 1330 |
| 「相手のことを考えた」 | 1390 |
| **「7月から1号店として」** | 1416 |
| 「10%を取るのは」 | 1374 |
| 「御社の規模は?」 | 1527 |
| 竹之内 教博(48) / 谷本 吉紹(47) / 関口 ケント(36) 株式会社 Webnesday 代表 | 800 / 1362 / ショート3秒 |

**★ASRの「15.3」は「1号店」の誤り。** テロップ「7月から1号店として」で判明。
**15社ではなく1号店ひとつ。** カードに「15社」と書かなくて済んだ。

**★加盟金150万円だけは画面に出ない。** 音声のみ（複数の話者が繰り返し、
300＝150＋150 と内部で整合するので採用したが、**サムネとタイトルには使っていない**）。

**サムネの被り**: 初回 445px → `faces[1].bias` 0.24→0.22 で 48px → **0.20 で 0px**。

### 2026-09-07-iwata-personal（9/11）

素材 `AwIGr3RKS00` 岩田繁人（関西版 令和の虎 81人目・58.2分）。出張型パーソナルジムの店舗化。

| 項目 | 値 |
|---|---|
| 長尺 | 786.56–1714.52（**928.0s / 15:28**）論点カード42枚 |
| ショート | 1135.96–1206.66（**70.7s**）＝元動画 **18.93分** |
| 冒頭 | 虎の断定「その大会で優勝してビッグ3専門店のパーソナルジムをやってターゲット20代から30代にしたら多分来るす」 |
| head | 「ビッグ3専門店で／20〜30代を狙え」**巻き戻し0秒** |
| 判定 | **ナッシング（不成立）** |

**★この回は酷評が非常に強い**（「夢のないビジネスプラン」「センスなし」は番組自身の帯）。
**代案（ビッグ3専門ジム・訪問営業）が入る区間を選んである。**
ガイドラインの「出演者の名誉を害する編集」を避けるため、
**酷評だけを並べた区間（986/1032/1061 の候補）は意図的に外した。**

**焼き込みテロップで裏取りしたもの**

| 出た文字 | 秒 |
|---|---|
| **「希望額160万円／残り160万円」** | 全編 |
| 「他の人の方が頑張ってる」 | 990 |
| 「BIG3専門パーソナルジム」 | ショート3秒（**head と一致**） |
| ドバシー社長(38) ミダシー株式会社 代表 | 1130 |
| 竹之内 教博(48) | 1400 |

**サムネ**: 被りは初回から 0px。ただし**右パネルに元の「詳しくは概要欄まで」帯が残った**
ので `faces[1].crop_top` を 0.12→**0.20** にして消した。

### 2026-09-07-iwaya-tanpen（9/12）

素材 `R05BBh55p_U` 岩谷文雄（二軍版 令和の虎 23人目・59.7分）。秋田発の30分短編映画。

| 項目 | 値 |
|---|---|
| 長尺 | 836.0–1650.0（**814.0s / 13:34**）論点カード37枚 |
| ショート | 1177.919–1247.44（**69.5s**）＝元動画 **19.63分** |
| 冒頭 | 虎の代案「であればそのショートドラマでYouTubeで、5分とか、20分30分見ない人が多いですから」 |
| head | 「20分30分は／もう見られない」**巻き戻し0秒** |
| 判定 | **完全ALL成立** |

**焼き込みテロップで裏取りしたもの**

| 出た文字 | 秒 |
|---|---|
| **「希望額100万円／残り100万円」** | 全編 |
| **「希望額100万円／残り70万円」** | 1614（**30万円が積まれたことを独立に裏付けた**。100−70＝30） |
| 「ファン層は違うと思う」 | 1210 |
| 梅田 鉄兵(49) 株式会社ファーマスター 代表取締役 | 1182 |
| 三原 拓大(26) 株式会社シュビヒロ 代表 | 1300 |
| トゥース馬場 / 馬場 達也(36) TaCファミリー歯科グループ 代表 | 300 |

**★この回はサムネの両パネルとも虎にした。**
**志願者（岩谷さん）の顔が抜けるフレームを見つけられなかった**ため。
5箇所（990/1150/1210/1300/1470/1530/1614）を抜いたが全部虎だった。
**発言の取り違えを避けるほうを優先し、確実に本人が言ったと分かる虎2人の引用にした**
（左＝梅田さんの「レッドカーペット歩きたい?」、右＝1210のテロップ「ファン層は違うと思う」）。
**次にこの素材を使うときは、志願者のフレームを先に探すこと。**

## 検証済みの事実（実際に画面に出した出力）

- **テスト 215 passed**（統合しようとしているツリーで実行）
- **3本とも `--dry-run` を長尺・ショート両方で通してから本ビルドした**
- **3本ともショートの巻き戻しは0秒**（ドライランに巻き戻しの行が出ない＝文頭に着地）
- **3本ともビルド後のショートから3秒のフレームを抜いて目で見た。**
  head と実際の発話・元テロップが一致していることを確認
- **3本ともサムネPNGを目で見た。** 頭切れ・人物取り違え・吹き出しの被りなし。
  **被り検査は3本とも0px**
- **`fixes` は全部ショート区間でHITすることを数えてから焼いた**
  （`iwata` の「パワリフティング」だけ当たらなかったので削除した）
- **台帳で6本とも `thumbnail_set = True` / `privacy_status = private` / `publish_at` 入りを確認**

| レシピ | 動画ID | 公開予定 | 尺 |
|---|---|---|---|
| `2026-09-07-yamamoto-caremane` | `jsaJeI_9dXs` | 2026-09-10T03:10:00Z | 844.5s |
| `2026-09-07-yamamoto-caremane-short` | `RdtP-7QPUaQ` | 2026-09-10T03:00:00Z | 70.9s |
| `2026-09-07-iwata-personal` | `xHL66uDgLPc` | 2026-09-11T03:10:00Z | 928.0s |
| `2026-09-07-iwata-personal-short` | `9tk9hplrqYU` | 2026-09-11T03:00:00Z | 70.7s |
| `2026-09-07-iwaya-tanpen` | `_1Ah8QyAKTI` | 2026-09-12T03:10:00Z | 814.0s |
| `2026-09-07-iwaya-tanpen-short` | `Ewv59mx4iVU` | 2026-09-12T03:00:00Z | 69.5s |

## 未検証のもの

- **9/5〜9/12 の予約が予定時刻に公開されるかは当日まで分からない**（9/4分までは予定どおり公開された）
- **今日の長尺3本は通しで見ていない。** 見たのはビルドログ、ffprobe の尺、サムネPNG、
  ショートの実映像1フレーム、テロップ裏取り用の静止画だけ
- **論点カードを映像と突き合わせていない。** 今日の3本（39+42+37＝118枚）は
  **`--every 22` のバケット開始秒をそのまま `at` に使い、各カードの前後の字幕を
  テキストで読んで内容が合っていることは確認した**が、**ビルド後の映像との
  突き合わせはしていない**
- **`iwaya-tanpen` の右パネルの人物が誰かは確定していない**（虎であることは確か）

## 次にやること

### 1. 9/13 以降を作る（クォータ回復は JST 16:00）

**在庫の素材はまだある。** 今日落とした4本のうち **`HowwrqNF9gw`（VEALL・関西版80人目・71.3分）は
まるごと手つかず**。ほか、山本・岩田・岩谷の3本も使ったのは15分前後だけなので未使用区間が残る。

**★`HowwrqNF9gw` は扱いに注意。** 「売名やろ？」と福祉を掲げる3人組を虎が追及する回で、
**そのまま切ると名誉を害する編集になりやすい。** 使うなら代案・構造整理の入る区間を選ぶこと。

**駒澤 `xfr6w7g_Xuc` の 23.0–34.5分も使える**（今日読んだ）。
属人化の詰め・リターンの詰め・「ワクワクしてない」・中村社長70万円→トータル160万円。
**ただし 34分以降は41歳未婚・恋愛遍歴の追及なので必ず外すこと**（虎自身が「偏見」と言っている）。
ショートの候補は `1549.279`（虎の問い「駒沢さんが全く現場に入らなかったらどういう影響が出てくると思います?」・
文頭着地・尺候補 65.7〜70.8s）。**ただし 25.8分で15〜25分帯をわずかに外れる。**

### 2. ショート2本目を埋める（実装は済んでいる）

**今日の3本はまだショート1本ずつ。** 運用（長尺1・ショート2）に合わせるには
各レシピに `shorts` を配列で書き足す。**親の長尺が公開済みのものから出すこと。**

`yamamoto-caremane` の2本目の候補は **`1136.840`（18.95分・67.9s）**
「フランチャイズで広げていくんじゃない。だから僕ね、福祉のフランチャイズで正直イメージがあんま良くなくて」
＋「なんか説明やばくないですか?大丈夫?」。**レターボックス（1380〜1406）に掛からないことは確認済み。**

### 3. 公開日ごとに再生リストとカード

**予約公開の動画は private なので `sync_playlists.py` は入れない。公開日にやる。**

| 日 | 長尺 | 入れる再生リスト | 再生リスト | カード |
|---|---|---|---|---|
| 9/1〜9/4 | — | — | 済 | 未 |
| 9/5 | `JOBx3jI5Xvw` | 不成立 | 未 | 未 |
| 9/6 | `teRlKkX00c4` | 不成立 | 未 | 未 |
| 9/7 | `BIB8bQxXzj4` | 協力確約 | 未 | 未 |
| 9/8 | `CpjcMM1RMXk` | 完全ALL成立回 | 未 | 未 |
| 9/9 | `HllE-g7dgXU` | 不成立 | 未 | 未 |
| **9/10** | **`jsaJeI_9dXs`** | **協力確約** | 未 | 未 |
| **9/11** | **`xHL66uDgLPc`** | **不成立** | 未 | 未 |
| **9/12** | **`_1Ah8QyAKTI`** | **完全ALL成立回** | 未 | 未 |

```bash
python scripts/sync_playlists.py
python scripts/sync_playlists.py --apply
```

### 4. 登録者を増やす仕掛け（未着手）

5万再生で7人（0.014%）、共有0、コメント4。**ショートは単発で完結していて、
次を見る理由が画面に一度も出ていない。** ここは設計そのものが無い。

### 5. TikTok

**アカウント作成は大島さんの作業。** 設定内容は
[`docs/2026-08-31-tiktok-account-setup.md`](docs/2026-08-31-tiktok-account-setup.md) に用意済み。
照会メール（`get-clip@razil.jp` 宛）は未送信。方針は「返事を待たずに投稿する」（2026-08-31）。

## 触ってはいけないところ・保留中の判断

**ワークツリーは main から分岐していることがある。今回で6回連続、
作業前に `git reset --hard main` が要った。最初に `git log --oneline -3` を見ること。**

**書き込む系（upload / sync_playlists --apply）は本体から実行する。**
`upload_youtube.py` は `__file__` で台帳の場所を決める。
**`work/` はジャンクションで共有だが `scripts/` は共有されない。**

**アップロードは1日6本まで**（1,600×6＋サムネ50×6＝9,900）。**回復は JST 16:00。**
**`--schedule` は未アップロードの動画に直接付けること**（先に上げてから入れ直すと
`videos.update` の50が1本ごとに余分にかかり、6本で10,200になって超過する）。

**レシピを作る手順**（今日もこの順でやった）

1. `show_subs.py <id> <開始> <終了> --every 120` で流し読みして山場を探す
2. **既存レシピと台帳で未使用区間を確認する**
3. **ショートの候補を先に総当たりで出す**（下のコード）。**65〜73秒に収まる組があるかを先に見る**
4. **候補の前後でレターボックスを測る**（上記）
5. **タイトル・金額・固有名詞は必ず焼き込みテロップで裏取りする**
   （`ffmpeg -ss <秒> -i work/<ID>/source.mp4 -frames:v 1 out.jpg` で抜いて目で読む）
6. `show_subs.py <id> <開始> <終了> --every 22` で流し読みし、**バケットの開始秒をそのままカードの `at` に使う**
7. `--dry-run` を長尺・ショート両方で通す
8. 本ビルド → **サムネPNGを目視** → 被りが出たら `bias` を0.02刻みで下げて `--thumb-only`
9. **ショートは必ずビルド後の動画からフレームを抜いて確認する**

```python
# -*- coding: utf-8 -*-
import json,sys
sys.path.insert(0,'.')
from scripts.moments import rewind_to_topic_head
cues=json.load(open(f'work/{SID}/subs.json',encoding='utf-8'))
for c in cues:
    t=c['t']
    if not (LO<=t<=HI): continue                      # 15〜25分帯の秒数
    landed=rewind_to_topic_head(t,cues)
    if landed['kind']!='文頭' or abs(landed['start']-t)>1e-6: continue
    ends=[x['t'] for x in cues if t+65<=x['t']<=t+73]
    if not ends: continue
    print(f"開始 {t:9.3f} 尺候補 {[round(e-t,2) for e in ends]}")
    print('   ', c['line'][:60])
```

**★論点カードの `at` は等間隔で置かない。** `--every 22` のバケット開始秒を使う。
**1枚が2行に収まらないと本ビルドが中断する。判定カードの `detail` は3行まで**
（今日 `iwata` で踏んだ。38文字に詰めて通した）。

**★サムネの被りは目視で判定しない。** `build_clip.py` が毎回自動で検査する。
`bias` を 0.02刻みで下げ、0になった最初の値を採る。**`x` は動かさない。**
**`--thumb-only` は再エンコードを挟まないので安く回せる**（被り検査も走る）。

**★元テロップが残ったら、下なら `crop_bottom`、上なら `crop_top` を上げる**
（今日 `iwata` の右パネルで上の帯が残った。`crop_top` 0.12→0.20 で消えた）。

**★カードを根拠に虎の名前を書かないこと。** 挿入プロフィールカードは
直後の発言者と一致しないことがある。**例外は、番組が同じ瞬間にテロップと氏名カードの
両方を出しているとき**（今日は `iwata` のドバシー社長、`iwaya` の梅田さんがそれ）。

**`fixes` は順番に単純置換される。短い方が後だと二重に当たる。**
**キューをまたぐ文字列は一致しない。** 当たるかどうかは事前に数えられる。

**長尺には自前の字幕を焼かない。`fixes` が効くのはショートの字幕だけ。**

**Bash ツール経由で Python の heredoc を書くときの落とし穴**
- stdin の既定エンコーディングが cp932。**先頭に `# -*- coding: utf-8 -*-` が要る**
- **`\n` はツールを通る間に実改行になる。** 改行は `chr(10)`
- **出力が化けるときは `sys.stdout.reconfigure(encoding='utf-8')`**
- **長文ファイルは Write ツールで書く**
- **`build_clip.py` の警告は日本語が化けて出ることがある。** 化けたら `--dry-run` を単独で流し直す

**関連動画は今も0件のまま。** 原因不明（POST は 200、`no_match_illustration` が返る）。

**重複アップロード `yHxbK4Y05zI`（`NSAi-BfomiU` と同内容）が残っている。
削除は取り返しがつかないので相談してから。**

**8/26公開の `Atsm_3CKLx8` は焼き込み字幕が訂正前のまま。** 差し替えは取り下げになる。

**音を聞かないと確定しないASRの崩れが残っている。推測で `fixes` に足さないこと。**
今日の3本で残したもの:

| レシピ | 残っている崩れ |
| --- | --- |
| `yamamoto-caremane` | 「かに取るか」（加算をどう取るか か）、「毛山マネ」（ケアマネ か。ショート区間外なので触っていない）、「FC拾隊」（FC商隊 か） |
| `iwata-personal` | 「エアロビクスの要請」（養成 か）、「妖怪ご高い方」（要介護度の高い方 か）、「イコマシ」（生駒市）、「1万2以上」（1万通以上 か） |
| `iwaya-tanpen` | 「女監督」（助監督 か。**ショート区間内に残っている**）、「盛坂」（盛んに か）、「執年映画」（短編映画 か）、「脚音」（脚本 か） |
