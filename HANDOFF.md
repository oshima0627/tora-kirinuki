# 引き継ぎ

最終更新: 2026-08-28

## いま何をしているのか

**在庫の素材から新しい動画を3日分作り、9/1 まで予約投稿を埋めた。**

きっかけはアナリティクスの確認だった。**8/27 に入れた施策は今日は測れない**
（Analytics のデータが API で 08-25、Studio で 08-26 までしか無い）。
測れないぶん手を動かし、**判定が付く長尺17本すべてにカードを入れ**、
**8/30・8/31・9/1 の3日分を新規に作って予約した**。

**「8/30以降は素材が無い」は誤りだった。** 落としてある元動画11本に
未使用区間が5時間8分残っていて、そこから作った。**まだ6件残っている。**

## 今回やったこと

| やったこと | 結果 |
|---|---|
| ワークツリーを main に fast-forward | それ以前は `scripts/` が空だった |
| アナリティクスを API と Studio の両方で確認 | 下記「検証済みの事実」 |
| `sync_playlists.py --apply` | 8/28公開分2本を追加。再実行で 0/0/0ユニット |
| `report_stats.py` に**データ最終日**と**カード指標**を追加 | pytest 176 passed |
| 関連動画をもう一度試す | **今日も0件。詰まったまま** |
| **カードを長尺10本に追加** | 判定が付く長尺は全部埋まった（17本） |
| **素材の在庫を数えた** | 未使用の検出済み候補が9件あった |
| **レシピ3本を新規作成・ビルド・予約投稿** | 長尺3本＋ショート3本＝6本、サムネも全部通った |

## 検証済みの事実（実際に画面に出した出力）

### 予約できたもの（台帳と動画IDで確認）

| 公開予定（JST） | 種類 | 動画ID | タイトル |
|---|---|---|---|
| 8/30 12:00 | ショート | `5cvw_zqL52g` | 沈みゆく船に／人を乗せるな |
| 8/30 12:10 | 長尺 | `-_kvvlKpvfA` | 虎「沈みゆく船に人を乗せたら、みんな不幸になる」志願者「絶対に沈ませません」 |
| 8/31 12:00 | ショート | `0V9WKZLW2Dw` | 利益を削ってまで／安く売る理由 |
| 8/31 12:10 | 長尺 | `VgVtuZMdFQk` | 虎「共同購買だ」志願者「そうなんです、実は共同購買なんです」 |
| 9/1 12:00 | ショート | `FB5VHr1nKcY` | 絶対まずいと／言い切った虎が |
| 9/1 12:10 | 長尺 | `v4jVPOkfPkU` | 虎「俺食べたくないもん」→実食して「臭さが全くない」 |

**6本すべて `thumbnail_set=True`。** クォータは 1,600×6＋50×6＋再生リスト100で
ほぼ上限ちょうどだったが、403 は出なかった。

### 新規3本の素材と判定

| レシピ | 素材 | 区間 | 尺 | 判定 |
|---|---|---|---|---|
| `2026-08-28-higaki-shizumu` | `xtJI21E2-xE` | 2699.6–3582.0 | 14:42 / 短67.7秒 | ナッシング（不成立） |
| `2026-08-28-hayama-kyoudou` | `vi5HC54UaQM` | 71.0–972.0 | 15:01 / 短62.1秒 | 完全ALL（成立） |
| `2026-08-28-akutsu-jisshoku` | `79U5ErgT_Js` | 453.5–1341.0 | 14:47 / 短63.3秒 | 協力確約 |

- **金額・氏名・引用は焼き込みテロップで裏取りした**:
  「羽山 貴章（31歳）」「希望額200万円／残り170万円」「希望額800万円／残り750万円」
  「共同購買だ」「俺食べたくないもん」「臭さが全くない」「本当に危ないと思う」
- **裏取りできなかった固有名詞は `fixes` に足していない**
- サムネイルは3本とも PNG を目視確認。頭切れ・人物取り違え・吹き出しの被りは無し
- ショートは3本とも**実際にビルドした動画からフレームを抜いて確認した**

### アナリティクス

**データが施策日（8/27）に届いていない。** API の `dimensions=day` は
**08-25 で行が終わる**。Studio の「過去28日間」は 2026/07/30〜2026/08/26。
`cardImpressions` も `RELATED_VIDEO` も `YT_PLAYLIST` も 0 だが、**評価にならない**。

| 区分（〜08-25） | 視聴 | 視聴分 | 平均視聴 | 登録 |
|---|---|---|---|---|
| 長尺 18本 | **28** | 84 | 181秒 | +1 |
| ショート 18本 | **28,696** | 11,112 | 39秒 | +4 |

- 長尺のトラフィック: `YT_SEARCH 18` `EXT_URL 4` `NO_LINK_OTHER 3` `YT_CHANNEL 3`
- Studio「動画」フィルタ全期間: 視聴36 / **インプレッション381** / **CTR 4.7%** / 平均2:32
- Studio の発見経路: YouTube検索52.8% / **関連のショート動画16.7%** / 直接11.1% /
  外部11.1% / チャンネルページ8.3%。**関連動画・再生リストはグレーアウト（0）**
- チャンネル統計（リアルタイム）: 登録者 **6** / 総再生 36,454 / 動画 36

### カード（Studio で10本に追加）

すべて 0:45 起点・ティーザー付き。**10本すべて「保存」がグレーになったのを目で確認。**
`9ngQ_04cv0k` `VBYY-SU03uM` `_COv48LWvT0` は**再読み込みして残存を確認**した。

| 指す先 | 動画 |
|---|---|
| 完全ALL成立回 | `EDRPTR5yGUs` `VBYY-SU03uM` `9ngQ_04cv0k` `K_eAlE4vS6o` `_COv48LWvT0` `NSAi-BfomiU` |
| 不成立 | `RSyPpzXCUiI` |
| 協力確約 | `QFKFzKi3zuo` `3Be5bKiduVY` `O3_81hOC0j4` |

ティーザーは既存カードに合わせた:
完全ALL=「完全ALLで決まった回まとめ」／不成立=「不成立になった回まとめ」／
協力確約=「協力確約で終わった回まとめ」。

入っていないのは `9L95DRUbMWg`（`mai-teian`・判定が分類に当たらない）1本だけ。これは正しい。

### 再生リスト（API で再取得して確認）

| ID | 本数 | 名前 |
|---|---|---|
| `PLJ2GVKaPKNKI` | 20本 | 完全ALL成立回 |
| `PLLKRI2G5rY1o` | 8本 | 不成立・虎が詰めた回 |
| `PLYr3WAVLad7E` | 6本 | 協力確約で終わった回 |

### コード

`report_stats.py --traffic` に追加:

```
データが入っている最終日: 2026-08-25（今日は 2026-08-28）
```

カード指標（`cardImpressions` `cardClicks` `cardClickRate`
`cardTeaserImpressions` `cardTeaserClicks`）も出る。**Studio を開かなくても測れる。**
`python -m pytest tests/ -q` → **176 passed**。

## 次にやること

### 1. 公開日ごとに再生リストへ流し込む＋カードを1枚入れる

**予約公開の動画は private なので `sync_playlists.py` は入れない。公開日にやる。**

| 日 | 長尺 | 入れる再生リスト | ティーザー |
|---|---|---|---|
| 8/29 | `B9iCtgqAZaY` | 不成立 | 不成立になった回まとめ |
| 8/30 | `-_kvvlKpvfA` | 不成立 | 不成立になった回まとめ |
| 8/31 | `VgVtuZMdFQk` | 完全ALL成立回 | 完全ALLで決まった回まとめ |
| 9/1 | `v4jVPOkfPkU` | 協力確約 | 協力確約で終わった回まとめ |

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/sync_playlists.py
python scripts/sync_playlists.py --apply
```

カードは Studio で1本ずつ（手順は下の「触ってはいけないところ」）。

### 2. 残りの素材から作る（明日以降）

未使用の検出済み候補があと6件。**再ダウンロードは要らない。**
**アップロードは1日6本まで**（1,600×6＋サムネ50×6＝9,900ユニット）。
**クォータの回復は JST 16:00。**

| 元動画 | 区間 | score |
|---|---|---|
| `huV8XPdmcc4` 坂口 | 0:00:00〜0:15:05 | 149.0 |
| `w_3GLEVLQCQ` 堀江 | 0:04:49〜0:19:48 | 139.1 |
| `PqPxW3wYR7M` 河合 | 0:00:00〜0:14:56 | 128.8 |
| `xfr6w7g_Xuc` 駒澤 | 0:26:17〜0:41:15 | 115.2 |
| `DSCYmCBAp_I` Maiちゃん | 0:12:34〜0:27:39 | 86.7 |
| `TuYYB2N0JGE` 齋藤 | 0:12:11〜0:27:08 | 78.9 |

`VrgC54_ESTk` と `bah_FU172NA` は使い切っている。
**score は機械的な点数。中身に山場があるかは1本ずつ確かめること。**

### 3. 8/30 以降にアナリティクスを測り直す

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/report_stats.py --traffic
```

出力の先頭の「データが入っている最終日」が **2026-08-27 以降**になっていること。
見るのは `RELATED_VIDEO` / `YT_PLAYLIST` / `cardImpressions` /
Studio の「関連のショート動画」（いまは 16.7%）。

## 触ってはいけないところ・保留中の判断

**レシピを作る手順（今回3本で確立した）**

1. `work/<元動画ID>/subs.json` を時刻付きで読み、山場のある連続区間を選ぶ（13〜20分）
2. **タイトルの引用は必ず焼き込みテロップで裏取りする。**
   `ffmpeg -ss <秒> -i work/<ID>/source.mp4 -frames:v 1 out.jpg` で抜いて目で読む
3. 金額・氏名も同じ方法で裏取り。**ASRの数字と固有名詞は信用しない**
4. 同じ元動画の既存レシピがあれば、判定・氏名・希望額はそこから引き写せる
5. `--dry-run` を長尺・ショート両方で通してから本ビルド
6. **サムネ PNG を目視。** 元テロップが残ったら `crop_top` を上げて `--thumb-only`
7. **ショートは必ずビルド後の動画からフレームを抜いて確認する**

**ショートの巻き戻しが番組の挿入カードを巻き込むことがある（今回1回踏んだ）。**
`build_short` は開始点を文頭まで戻すので、`akutsu-jisshoku` では
1151.0 → 1137.0 まで戻って虎のプロフィール挿入（14秒）が頭に入ってしまった。
**ドライランの「巻き戻し」の行と、ビルド後の実映像の両方を見ること。**

**`fixes` は順番に単純置換される。短い方が後だと二重に当たる。**
「牛タ」→「牛タン」が既存の「牛タン」にも当たって「牛タンン」になった。
広い置換のあとに「牛タンン」→「牛タン」のような戻しを置く。

**カードを入れる手順（今日2回カードを2枚作ってしまった）**

```
studio.youtube.com/video/<ID>/edit
→ 右カラムを下にスクロールして「カード」の鉛筆を目で見つけて押す（位置は動画ごとに違う）
→ 「再生リスト」行の【＋】を押す（1回で開かないことがある。開かなければもう一度＋を押す。
   この時点ではカードは作られていないので安全）
→ ★選択ダイアログが開いたのをスクリーンショットで確認してからタイルを1回押す★
→ ★もう一度スクリーンショットを撮り、ダイアログが閉じたことを確認する★
→ 時刻を 0:45 に（書式は尺で変わる。4:32なら "0:45:00"、15:04なら "00:45:00"。
   入力後に【Tab】を押さないと確定しない）
→ ティーザーを入れる → 右上「保存」→ グレーになったのを確認
```

**★を飛ばすとカードが2枚できる。** 2枚目は真ん中のタイル（協力確約）の誤選択。
行のゴミ箱で消す。**タイル順は 完全ALL / 協力確約 / 不成立。**

**Studio に入るときは必ず
`studio.youtube.com/channel/UCWupHXqf8CTG00c_eoQSw1Q` から。**
既定チャンネルは別チャンネル。上部の「12.7K/16/4,012」は vidIQ の数字で登録者数ではない。

**書き込む系（upload / sync_playlists --apply）は本体から実行する。**
`upload_youtube.py` は `__file__` で台帳の場所を決めるので、ワークツリーから
実行するとワークツリー側の `state/published.json` に書かれて分岐する。
今回は本体から実行し、work ディレクトリだけ絶対パスでワークツリー側を渡した:

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
WT=C:/Users/oshim/Documents/projects/tora-kirinuki/.claude/worktrees/channel-settings-investigation-5fc148
python scripts/upload_youtube.py "$WT/work/<id>"       --schedule "2026-09-02T03:10:00Z"
python scripts/upload_youtube.py "$WT/work/<id>-short" --schedule "2026-09-02T03:00:00Z"
```

**順番は長尺→ショート。** ショートの概要欄は長尺のURLを台帳から引く。

**Bash ツール経由で Python の heredoc を書くときの落とし穴（今日3回踏んだ）**
- stdin の既定エンコーディングが cp932。**先頭に `# -*- coding: utf-8 -*-` が要る**
- **`\n` はツールを通る間に実改行になる。** 文字列に改行コードを入れたいときは
  `chr(10)` を使う（`report_stats.py` を1回壊した）
- Python の二重引用符の文字列に `"` を入れると構文エラー。`「」『』` を使う
- **長い heredoc を2つ繋げると bash が構文エラーを出す。**
  長文ファイルは Write ツールで書く

**`sync_playlists.py` は非公開の動画を入れない。** 予約公開分は公開日に流し直す。
`mai-teian` は判定が分類に当たらないので触らない。**推測で分類しない。**

**重複アップロード `yHxbK4Y05zI`（`NSAi-BfomiU` と同内容）が残っている。
削除は取り返しがつかないので相談してから。**

**8/26公開の `Atsm_3CKLx8` は焼き込み字幕が訂正前のまま。** 差し替えは取り下げになる。

**音を聞かないと確定しないASRの崩れが残っている。推測で `fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |
| `higaki-shizumu`（新規） | 「報道した方がいいのかな」（僭越ながらの直前） |
| `akutsu-jisshoku`（新規） | 「焼き鳥3級」の正式名、「ジムハイボール」 |
| `hayama-kyoudou`（新規） | 羽山さんの会社名（「株式会社山の」としか出ない） |

## 未検証のもの

- **予約した6本が実際に予定時刻に公開されるかは、当日まで分からない。**
  台帳と API の応答では `publishAt` が入っている
- **新規3本の長尺の本編（15分）は通しで見ていない。** 見たのは
  ドライランの字幕一覧、サムネPNG、ショートの実映像、長尺の1フレームだけ
- 8/27・8/28 に入れたものが数字を動かすかは不明。**8/30 以降に測る**
- **カード10本のうち再読み込みで確認したのは3本。** 残り7本は保存がグレーに
  なったことまで
- **Studio の「関連のショート動画 16.7%」が API のどのソース名に当たるかは未確認**
- **前回の「インプレッション 2,387」の出所は不明。** 今日は再現できなかった
- **終了画面が入っている長尺の全数は数えていない**（分かっているのは3本）
- **関連動画が0件になる原因は不明**（POST は 200、`no_match_illustration` が返る）
- 長尺の残存カーブ（`elapsedVideoTimeRatio`）は未測定
- 08-20 の配信断絶の原因は不明のまま
