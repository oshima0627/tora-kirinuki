# 引き継ぎ

最終更新: 2026-08-28

## いま何をしているのか

**アナリティクスを確認し、そのうえで手を動かせるものを片付けた。**

数字の結論は「**今日は測れない**」。Analytics に入っているデータは
**API で 2026-08-25、Studio で 2026-08-26 まで**しかなく、8/27 の施策日に届いていない。
0 が並ぶのは「効かなかった」ではなく「まだ集計されていない」。

測れないぶん、**埋まっていなかった導線を埋めた**。判定が付く長尺は
**全部（17本）にカードが入った**。8/28 公開分の再生リストも流し込み済み。

## 今回やったこと

| やったこと | 結果 |
|---|---|
| ワークツリーを main（6a3870b）に fast-forward | それ以前は `scripts/` が空だった |
| `report_stats.py --traffic` / Analytics API を直接叩く | 下記「検証済みの事実」 |
| Studio のアナリティクス > コンテンツを4フィルタすべて確認 | インプレッション・CTR を実測 |
| `sync_playlists.py --apply` | 8/28 公開分2本を追加。再実行で 0/0/0ユニット（べき等） |
| `report_stats.py` に**データ最終日**と**カード指標**を追加 | pytest 176 passed・実行して出力確認 |
| 関連動画をもう一度試す | **今日も0件。詰まったまま** |
| **カードを長尺10本に追加** | 判定が付く長尺は全部埋まった |

## 検証済みの事実（実際に画面に出した出力）

### いちばん大事なこと

**Analytics のデータは施策日（8/27）に届いていない。**

- API で `dimensions=day` を引くと **行は 08-25 で終わる**（08-26/27/28 は行が無い）
- Studio の「過去 28 日間」の表示は **2026/07/30〜2026/08/26**
- したがって `cardImpressions` も `RELATED_VIDEO` も `YT_PLAYLIST` も 0 だが、
  **これは施策の評価にならない**

`report_stats.py --traffic` の先頭にこの行を出すようにした:

```
データが入っている最終日: 2026-08-25（今日は 2026-08-28）
```

### API（最終日 2026-08-25）

| 区分 | 視聴 | 視聴分 | 平均視聴時間 | 登録 |
|---|---|---|---|---|
| 長尺 18本 合計 | **28** | 84 | 181秒 | +1 |
| ショート 18本 合計 | **28,696** | 11,112 | 39秒 | +4 |

- **長尺のトラフィック**: `YT_SEARCH 18` `EXT_URL 4` `NO_LINK_OTHER 3` `YT_CHANNEL 3`（計28）。
  **`RELATED_VIDEO` `YT_PLAYLIST` `SUBSCRIBER` は行が無い＝0**
- 再生場所: `SHORTS_FEED 28677` / `WATCH 31` / `EMBEDDED 1`
- カード指標 全期間: すべて 0
- チャンネル統計（リアルタイム）: **登録者 6** / 総再生 36,454 / 動画 36

### Studio（最終日 2026-08-26）

**「動画」フィルタ・全期間**（過去28日間と同値）:

| 視聴回数 | サムネイルのインプレッション数 | サムネイルのCTR | 平均視聴時間 |
|---|---|---|---|
| 36 | **381** | **4.7%** | **2:32** |

- 視聴者が動画を見つけた方法: YouTube検索 52.8% / **関連のショート動画 16.7%** /
  直接入力または不明 11.1% / 外部 11.1% / チャンネルページ 8.3%。
  **「関連動画」「再生リスト」はグレーアウト（0）**
- 「再生リスト」フィルタ: 「この期間には表示できるデータがありません」
- 「ショート」フィルタ: 視聴 3.4万 / エンゲージビュー 1.9万 / 高評価 69 / 登録 +2。
  **ショートにはインプレッション指標が無い**

### 入れたもの

**再生リスト（`sync_playlists.py --apply`）**

```
足す 2 本: [成立] NSAi-BfomiU / [成立] ji7bvZ1syLA
→ 再実行で 外す0・足す0・0ユニット
```

いまの中身（API で再取得して確認）:

| ID | 本数 | 名前 |
|---|---|---|
| `PLJ2GVKaPKNKI` | 20本 | 完全ALL成立回 |
| `PLLKRI2G5rY1o` | 8本 | 不成立・虎が詰めた回 |
| `PLYr3WAVLad7E` | 6本 | 協力確約で終わった回 |

**カード（Studio で10本に追加。すべて 0:45 起点・ティーザー付き）**

| 指す再生リスト | 動画 | ティーザー |
|---|---|---|
| 完全ALL成立回 | `EDRPTR5yGUs` `VBYY-SU03uM` `9ngQ_04cv0k` `K_eAlE4vS6o` `_COv48LWvT0` `NSAi-BfomiU` | 完全ALLで決まった回まとめ |
| 不成立 | `RSyPpzXCUiI` | 不成立になった回まとめ |
| 協力確約 | `QFKFzKi3zuo` `3Be5bKiduVY` `O3_81hOC0j4` | 協力確約で終わった回まとめ |

- **10本すべて「保存」がグレーになったのを目で確認した**
- **`9ngQ_04cv0k` `VBYY-SU03uM` `_COv48LWvT0` は再読み込みして残存を確認**
  （カード1枚・0:45:00・正しい再生リスト・ティーザーあり）
- ティーザーの文言は既存カード（`xlqEPCrF6cI` = 「完全ALLで決まった回まとめ」、
  `VwO47R4gGsE` = 「不成立になった回まとめ」）に合わせた

**これで判定が付く長尺は17本すべてカードが入った。**
入っていないのは `9L95DRUbMWg`（`mai-teian`・判定が分類に当たらない）1本だけ。これは正しい。

### コード

`scripts/report_stats.py` の `--traffic` に追加:

- **データが入っている最終日**（0 の読み違いを防ぐ）
- **カード指標** `cardImpressions` `cardClicks` `cardClickRate`
  `cardTeaserImpressions` `cardTeaserClicks`

`python -m pytest tests/ -q` → **176 passed**。実行して出力も確認済み。

### 分かったこと

- **`cardImpressions` は Analytics API から取れる。Studio は要らない。**
  前回の引き継ぎの「Studio 側で見る」は不要だった
- **サムネイルのインプレッション／CTR は API に無い。** Studio の
  アナリティクス > コンテンツ >「動画」フィルタだけ
- **前回の「インプレッション 2,387」は再現できなかった。** 動画=381、
  ショートにはインプレッション指標が無く、「すべて」にも出ない。
  **366（8/27）→ 381（8/28）は連続している。** 2,387 の出所は不明
- **関連動画（特定の動画の選択）は今日も0件。** `aPof751FH1U` / 「令和の虎」/「虎」
  いずれも「一致する結果がありません」。
  `POST /youtubei/v1/creator/list_creator_videos` は **200** を返し、
  `no_match_illustration` の画像を取りに行っている。**UI の失敗ではなく空の応答**

## 次にやること

### 1. 8/30 以降にもう一度測る（今日できなかったこと）

データが 08-27 に届いてから。

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/report_stats.py --traffic
```

**出力の先頭の「データが入っている最終日」が 2026-08-27 以降になっていること。**
そうでなければまだ測れていない。

| 見るもの | いまの値 |
|---|---|
| `RELATED_VIDEO`（長尺） | 0 |
| `YT_PLAYLIST`（長尺） | 0 |
| `cardImpressions` | 0 |
| Studio「関連のショート動画」 | 16.7% |

### 2. 8/29 公開分を流し込む（公開後に）

`yotsui-kakkoii`（不成立）の2本はまだ private。公開日に:

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/sync_playlists.py
python scripts/sync_playlists.py --apply
```

そのあと `B9iCtgqAZaY` にカードを入れる（不成立・0:45・「不成立になった回まとめ」）。

### 3. 関連動画は時間をおいてもう一度

2日連続で0件。YouTube 側の不具合の可能性が高い。

| ショート | 長尺 |
|---|---|
| `U9FMPUTeZ-Y` | `aPof751FH1U` |
| `ji7bvZ1syLA` | `NSAi-BfomiU` |
| `Yl3wV9uQnxU` | `B9iCtgqAZaY`（8/29公開予定） |

### 4. 終了画面

入っているのが分かっているのは `VwO47R4gGsE` `O3_81hOC0j4` `aPof751FH1U` の3本だけ。
**全数を数えていない。** カードと同じ要領で残りに入れる。

## 触ってはいけないところ・保留中の判断

**カードを入れる手順（今日2回やらかしたので手順で防ぐ）**

```
studio.youtube.com/video/<ID>/edit
→ 右カラムを下にスクロールして「カード」の鉛筆を目で見つけて押す（位置は動画ごとに違う）
→ 「再生リスト」行の【＋】を押す（行のラベルを押しても開かないことがある。
   開かなければもう一度＋を押す。この時点ではカードは作られていないので安全）
→ 再生リスト選択ダイアログが開いたのを【スクリーンショットで確認してから】タイルを1回押す
→ ★ここで必ずもう一度スクリーンショットを撮り、ダイアログが閉じたことを確認する★
→ 時刻を 0:45 に（時刻欄の書式は動画の尺で変わる。
   4:32なら "0:45:00"、15:04なら "00:45:00"。入力後に【Tab】を押さないと確定しない）
→ ティーザーテキストを入れる → 右上「保存」→ グレーになったのを確認
```

**★の確認を飛ばすとカードが2枚できる。** 2枚目は選択ダイアログの
真ん中のタイル（協力確約）が誤選択されたもの。できてしまったら行のゴミ箱で消す。
**再生リストのタイル順は 完全ALL / 協力確約 / 不成立。**

**Studio に入るときは必ず
`studio.youtube.com/channel/UCWupHXqf8CTG00c_eoQSw1Q` から。**
既定チャンネルは別チャンネル（「日本の最新ニュースまるわかり」）。

**Studio 上部の「12.7K / 16 / 4,012」は vidIQ 拡張の数字。登録者数ではない**（実際は6人）。

**`upload_youtube.py` は `__file__` で台帳の場所を決める。**
**書き込む系（upload / sync_playlists --apply）は本体
（`C:/Users/oshim/Documents/projects/tora-kirinuki`）から実行する。**
読み取りだけの `report_stats.py` はどちらでもよい。

**`sync_playlists.py` は非公開の動画を入れない。** 予約公開の分は公開日に流し直す。
`mai-teian`（判定が分類に当たらない）は触らない。**推測で分類しない。**

**重複アップロード `yHxbK4Y05zI`（`NSAi-BfomiU` と同じ内容）が残っている。
削除は取り返しがつかないので相談してから。**

**8/26公開の `Atsm_3CKLx8` は焼き込み字幕が訂正前のまま。**
差し替えは公開済み動画の取り下げになる。やるかどうかは相談してから。

**音を聞かないと確定しないASRの崩れが残っている。推測で `recipe.fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」「津田さん」「紫さん」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |

**8/30以降の公開枠は空**（新しく作る素材が無い）。予約は 8/29 まで。

## 未検証のもの

- **8/27・8/28 に入れたものが数字を動かすかは分からない。** 8/30 以降に測る
- **カード10本のうち再読み込みで確認したのは3本**
  （`9ngQ_04cv0k` `VBYY-SU03uM` `_COv48LWvT0`）。
  残り7本は「保存」がグレーになったことまでしか見ていない
- **カードが実際にプレーヤーに出ているかは見ていない**
- **Studio の「関連のショート動画 16.7%」が API のどのソース名に当たるかは未確認**
- **前回の「インプレッション 2,387」の出所は不明**
- **終了画面が入っている長尺の全数は数えていない**
- **関連動画が0件になる原因は不明**（通信は200・空の応答）
- 長尺の残存カーブ（`elapsedVideoTimeRatio`）は未測定
- 08-20 の配信断絶の原因は不明のまま
