# 引き継ぎ

最終更新: 2026-08-26

## いま何をしているのか

**ショートから長尺への導線を調べた。結論: 概要欄のリンクは全部入っているが、
Shorts の「関連動画」欄は1本も入っていない。そして今は入れられない。**

前回（同日）のアカウント調査で「ショート2.6万再生 → 長尺26再生」という漏斗が出た。
その原因の候補として、ショート側の導線を実物で確かめた。

- **概要欄のリンク: 20/20 入っている。** 台帳にある全ショートが、対応する自分の
  長尺のURLを持っている。ずれているものはゼロ
- **Shorts の「関連動画」欄: 全部「なし」。** Studio の画面で確認した
- **入れようとしたら止まった。** この欄は『上級者向け機能』で、有効化には
  **本人確認（自撮り動画／身分証明書／チャンネル履歴のいずれか1つ）**が要る。
  **これは本人にしかできない。こちらでは進めない**

概要欄が全部入っているのに `EXT_URL`（概要欄リンク経由）が期間中 **5再生**。
モバイル89%のショート視聴で概要欄はほぼ開かれていない。
「関連動画」欄はショートのプレイヤー本体に出るので、置き場所が根本的に違う。

## 今回やったこと

| ファイル | 何を |
|---|---|
| `scripts/audit_links.py` | 新規。ショートの概要欄に対応長尺のリンクがあるかを全数で出す。書き込みはしない |

Studio をブラウザで開いて `Atsm_3CKLx8` の詳細と機能の利用資格を見た。**何も保存していない**
（認証ダイアログはキャンセル、設定ダイアログは閉じるで抜けた）。

## 検証済みの事実（実際に画面に出したもの）

- `python -m pytest -q` → **172 passed**（今回テストは足していない）
- `python scripts/audit_links.py` の出力:
  - 動画48本 / 180秒未満 24本
  - 台帳にある20本すべて `○`（対応長尺のURLが概要欄にある）
  - `×` の4本（`ZpsydtIqHm4` `5LbLIA1YTw8` `wjoh-MPER6M` `FureBeVFerE`）は
    **台帳外の private。それぞれ台帳外の長尺を指している**ので、実質は張れている
  - `irfzXTmIqQQ`（差し替えで retired にした旧ショート）は `aPof751FH1U` を指す `△`
- `videos.list` を全パート（`snippet,contentDetails,status,statistics,player,
  topicDetails,recordingDetails,processingDetails,suggestions,localizations`）で
  引いた。**関連動画に相当するフィールドは存在しない**。API からは設定できない
- 公開ショート `Atsm_3CKLx8` の Shorts ページHTML: `relatedVideo` 0 / `videoAttribution` 0 /
  `sourceVideo` 0 / `shortsRelated` 0（`reelPlayerOverlay` は26箇所ある）
- **Studio の画面（スクリーンショットで確認）**:
  - 動画詳細の右カラムに **「関連動画 ⓘ / なし」＋鉛筆アイコン** がある
  - 鉛筆を押すと **「ワンタイム認証が必要です — ショート動画を動画にリンクさせて
    その他の高度な YouTube 機能を利用できるようにするには…」**
  - 次の画面で3択: **6秒の自撮り動画の撮影 / 身分証明書の写真の撮影 / 成長の履歴の構築**
    （最後は「アクティブな利用期間が2か月必要」と注記）
  - 設定 > チャンネル > 機能の利用資格:
    **1.標準機能 有効 / 2.中級者向け機能 有効 / 3.上級者向け機能「利用資格あり」**
    要件は「中級者向け機能が有効 ✓」＋「動画認証 / 有効な身分証明書 / チャンネル履歴」の
    いずれか1つ。`機能を利用する` ボタンがある
  - 上級者向け機能の一覧に **「動画の説明内の外部リンク」** も入っている
  - ダッシュボード: **登録者 4**（過去28日で 2 増加）。前回の3から増えた
  - `Atsm_3CKLx8`（8/26公開）: API時点で 再生927 / 高評価7、その数分後の画面で 932

## 次にやること

### 1. 上級者向け機能を有効にする（本人の作業）

**これをやらないと関連動画は1本も設定できない。** 一番速いのは自撮り動画（6秒）。

```
studio.youtube.com > 設定 > チャンネル > 機能の利用資格 > 3.上級者向け機能 > 機能を利用する
```

3択のうち「成長の履歴の構築」なら手を動かさなくてよいが、**2か月の利用期間**が要る。
チャンネル開設からの経過を考えると、待つ選択も現実的。どちらにするかは判断待ち。

承認は「通常、数時間で承認されます」と画面に出ていた。

### 2. 有効になったら、20本に関連動画を入れる

対応表は `python scripts/audit_links.py` の `○` の右に出る videoID がそのまま使える。
Studio の 動画 > 詳細 > 関連動画 で1本ずつ選ぶ。**API では設定できない**。

入れたら効果を測る。見るのは長尺側の `YT_RELATED` / 総再生（いまは合計26）。

### 3. 8/27〜8/29 の公開を見届ける

予約は 8/29 まで。**8/30以降の枠は空**（新しく作る素材が無い）。

```bash
python scripts/report_stats.py --retention
```

8/27公開の `U9FMPUTeZ-Y` が訂正版ショートの初回。比べる相手は 08-20以降の同じ配信条件の回
（`nIulWMDR7dQ` `Pi1SplRFskQ` `19nBjuV4evc` `BHhxA4jk7VU` `Db824zUyJ3c` `RveQoV1zud4`）。
**見るのは再生数ではなく 25%地点の残存と高評価率。**

### 4. 測っていない穴

- **長尺の残存カーブ**を `elapsedVideoTimeRatio` で引く。平均視聴時間 1:18 が
  「冒頭で切れている」のか「途中まで見て抜ける」のかで打ち手が変わる
- **カードと終了画面**を1本に入れて `cardImpressions` が0から動くか見る。
  いま0なのは効果が無いからではなく、**置いていないから**

## 触ってはいけないところ・保留中の判断

**本人確認（自撮り／身分証）はこちらでは絶対にやらない。** 本人の生体情報と
身分証明書なので、代行しない。

**音を聞かないと確定しないASRの崩れが残っている。推測で `recipe.fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」「津田さん」「紫さん」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |

`python scripts/build_short.py recipes/<id>.json --dry-run` で焼く字幕が全部出る。

**`published.json` に無い private 動画が8本ある。**（ショート4・長尺4のペア）
台帳に無いので差し替えも削除も手作業になる。すべて0再生。

```
ZpsydtIqHm4  cjeTzX5kFIw  5LbLIA1YTw8  xcrKdusnOz0  wjoh-MPER6M  -whIQ-8kiaw  (08-10)
op5uGCFQJ5s  FureBeVFerE                                                       (08-13)
```

**`upload_youtube.py` はカレントディレクトリで台帳の場所が変わる。**
`ROOT = Path(__file__).parents[1]` なので、ワークツリーから実行すると
ワークツリー側の `state/published.json` を見る。本体側と食い違ったまま両方から
上げると、**同じ動画が二重にアップロードされる**（`yHxbK4Y05zI` で起きた）。
**実行は本体（`C:/Users/oshim/Documents/projects/tora-kirinuki`）からに統一する。**

**8/26公開の `Atsm_3CKLx8`（akutsu-hantei-short）は焼き込み字幕が訂正前のまま。**
差し替えるなら公開済み動画の取り下げになるので、やるかどうかは相談してから。

**引き継ぎはフックで強制されている。** `SessionStart` でこのファイルが context に流し込まれ、
`Stop` で未pushかつ `HANDOFF.md` 未更新なら止まる。中身は `.claude/hooks/`、
判定のテストは `tests/test_hooks.py`。

## 未検証のもの

- **「関連動画」を入れたら長尺の再生が動くかは、当然まだ分からない。** 認証が通っていない
- **HTMLに関連動画のマーカーが無い＝未設定**、と読んだが、**設定済みショートを見て
  比べていない**。マーカー名が別の可能性は残る（Studio の画面が「なし」と出しているので
  結論自体は変わらない）
- **08-20 の配信断絶の原因は不明のまま。** ファイル仕様は前後で同一
  （mov / 1080x1920 / 30fps / h264 / aac 2ch）。こちら側の変更では説明できない
- 全動画に `suggestions.processingHints: ["nonStreamableMov"]`（faststart 未適用のMOV）。
  `processingStatus` は `succeeded`。**実害があるかは確かめていない**
- 「視聴者の種類（新規/リピート）」は Analytics API が 500 を返して取れない
- **今日のクォータ消費は測っていない。** 今回の読み取りは十数ユニット。
  API は使用量を返さないので、確かめるなら Google Cloud コンソールの
  「YouTube Data API v3 > 割り当て」を見る。PT日が変わるのは **JST 16:00**
