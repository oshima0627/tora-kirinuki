# 引き継ぎ

最終更新: 2026-08-27

## いま何をしているのか

**Studio の設定を一通り見て、埋まっていない穴を洗い出した。書き込みはしていない。**

配信を妨げる設定は今回も1つも無い。問題は設定ではなく、
**視聴者を次に運ぶ部品（終了画面・カード・再生リスト・透かし・関連動画）がほぼ全部空**なこと。
長尺は全期間でインプレッション **366回**、視聴 **28回**、登録者は **4人**。

詳細と根拠は `docs/2026-08-27-studio-audit.md`。

## 今回やったこと

コードの変更なし。追加したのは調査ドキュメント1本。

| ファイル | 何を |
|---|---|
| `docs/2026-08-27-studio-audit.md` | 新規。Studio の全設定画面の実測と、空のままの部品12個 |

実行したコマンド: `python scripts/report_stats.py --traffic` / `python scripts/audit_account.py`。

## 検証済みの事実（実際に画面に出したもの）

- **登録者は 4人。** アナリティクス > リアルタイム >「現在の数を表示」で確認。
  **Studio 上部の「187」は vidIQ 拡張の数字で、登録者数ではない**
- **ブラウザの既定チャンネルは別チャンネル**（「日本の最新ニュースまるわかり」）。
  `studio.youtube.com/` を素で開くとそちらが出る。
  必ず `studio.youtube.com/channel/UCWupHXqf8CTG00c_eoQSw1Q` から入る
- 機能の利用資格は **1・2・3 すべて有効**（上級者向けも通っている）
- 空のままの部品（すべて画面で目視）:
  - **動画の透かし 未設定**（長尺プレイヤーの登録ボタンが出ていない）
  - **リンク 0件**・連絡先メール 空
  - **終了画面ゼロ**（`aPof751FH1U` で開くとテンプレート選択が出る＝要素なし）
  - **カードなし**
  - **`aPof751FH1U` はどの再生リストにも属していない**（欄が「選択」）
  - **再生リストは2本だけ、最終更新 08/12・08/13 で停止、視聴回数はどちらも 0**
  - ホームタブに**登録者向けの「再視聴用動画」が未設定**。空セクション3つが残っている
  - **ショート `U9FMPUTeZ-Y` の関連動画が「なし」**
  - コンテンツ一覧の**通知列が全行「—」**（予約公開では登録者に通知が飛んでいない）
  - 長尺に**字幕ファイルなし**
  - **コメント3件すべて返信ゼロ**、固定コメントなし
- 数字（過去28日・2026/07/29〜08/25）: 視聴 2.9万 / 総再生 186.6時間 / 登録 +4。
  視聴の内訳は **ショート 99.9% / 動画 0.1%**
- 数字（**全期間・「動画」フィルタ**）: 視聴 28 / インプレッション **366** / CTR 4.9% / 平均視聴時間 3:01
- トラフィックソース: `SHORTS 25217` / `YT_SEARCH 402` / `YT_OTHER_PAGE 264` /
  `YT_CHANNEL 124` / `EXT_URL 6` / `SUBSCRIBER 4`。**`RELATED_VIDEO` は0（行が出ない）**
- 収益化（データは 8/21 時点）: 有効な総再生時間 **0時間**/3,000、
  有効なショート視聴 5,436/300万、登録者 4/500
- **台帳に無い private が 10本に増えた**（前回8本）。
  増えたのは `irfzXTmIqQQ` と **`yHxbK4Y05zI`**
- **`yHxbK4Y05zI`（8/26・904秒・private）は `NSAi-BfomiU`（8/28予約・904秒）の重複アップロード。**
  Studio のコンテンツ一覧に同タイトル・同尺で2行並んでいる

## 次にやること

### 1. `U9FMPUTeZ-Y` の関連動画 —— 今日は設定できなかった

**「特定の動画の選択」ダイアログが何を検索しても0件を返す。** 2026-08-27 の実測:

| 入れた検索語 | 結果 |
|---|---|
| `aPof751FH1U`（対の長尺・本日12:10公開・公開中） | 一致する結果がありません |
| `O3_81hOC0j4`（8/26公開の長尺。**昨日はIDで1件ヒットした**） | 一致する結果がありません |
| `Pi1SplRFskQ`（ショート） | 一致する結果がありません |
| `令和の虎`（全48本のタイトルに入っている語） | 一致する結果がありません |

ページを再読み込みし、ダイアログを開き直しても同じ。
**通信は失敗していない**: `POST /youtubei/v1/creator/list_creator_videos` は **200** を返し、
その上で「一致する結果がありません」の画像が読み込まれている。**YouTube 側が0件と答えている。**

`U9FMPUTeZ-Y` の関連動画は**「なし」のまま**。保存ボタンはグレー＝何も変更していない。

**前回の引き継ぎに書いた「IDを入れると候補が1件だけ出る」は、今日は再現しない。**
原因は不明。時間をおいてもう一度開くこと。開いたら:

```
studio.youtube.com/video/U9FMPUTeZ-Y/edit
→ 右カラム「関連動画」の鉛筆 → 検索欄に aPof751FH1U
→ 出た1件をクリック → 右上「保存」（グレーになれば保存済み）
```

残り2本も公開日に同じ作業:

| ショート | 長尺 | 長尺の公開 |
|---|---|---|
| `ji7bvZ1syLA` | `NSAi-BfomiU` | 2026-08-28 12:10 JST |
| `Yl3wV9uQnxU` | `B9iCtgqAZaY` | 2026-08-29 12:10 JST |

### 2. 長尺に終了画面とカードを1本入れて、0から動くか見る

いまカードのインプレッションが0なのは**置いていないから**。効果の有無はまだ誰も知らない。
1本だけ入れて `cardImpressions` と終了画面のクリックを見る。

### 3. 再生リストを再開する

8/13以降の長尺（約15本）はどの再生リストにも入っていない。既存の2本も視聴0。
長尺の連続再生＝総再生時間を稼げる唯一の仕掛けがいま死んでいる。

### 4. 透かし（登録ボタン）を設定する

カスタマイズ > プロフィール > 動画の透かし。150x150 の画像。長尺でしか出ないが、
長尺は「見た人が登録する率」で見ると唯一プラスが出ている経路（28日の登録 +1 は長尺から）。

### 5. 重複アップロード `yHxbK4Y05zI` をどうするか決める

`NSAi-BfomiU`（8/28公開予約）と同じ内容。0再生・private。
**削除は取り返しがつかないので、判断は相談してから。**

### 6. まだ埋めていない穴

- 前回の「インプレッション 2,387」と今日の「366」が**6.5倍食い違っている**（→ §5）。
  次に見るときは「動画」フィルタであることを画面で確かめてから記録する
- 長尺の残存カーブ（`elapsedVideoTimeRatio`）は未測定のまま

## 触ってはいけないところ・保留中の判断

**Studio の保存はダイアログが閉じきる前に押しても効かない。** 動画を選んだ直後に「保存」を
押すとオーバーレイに吸われて無反応のまま。**グレーになったことを必ず目で確認する。**

**`upload_youtube.py` はカレントディレクトリで台帳の場所が変わる。**
`ROOT = Path(__file__).parents[1]` なので、ワークツリーから実行するとワークツリー側の
`state/published.json` を見る。**実行は本体（`C:/Users/oshim/Documents/projects/tora-kirinuki`）
からに統一する。** `yHxbK4Y05zI` はこれで二重に上がった可能性が高い。

**音を聞かないと確定しないASRの崩れが残っている。推測で `recipe.fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」「津田さん」「紫さん」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |

`python scripts/build_short.py recipes/<id>.json --dry-run` で焼く字幕が全部出る。

**8/26公開の `Atsm_3CKLx8`（akutsu-hantei-short）は焼き込み字幕が訂正前のまま。**
差し替えるなら公開済み動画の取り下げになるので、やるかどうかは相談してから。

**8/30以降の公開枠は空**（新しく作る素材が無い）。予約は 8/29 まで。

**引き継ぎはフックで強制されている。** `SessionStart` でこのファイルが context に流し込まれ、
`Stop` で未pushかつ `HANDOFF.md` 未更新なら止まる。中身は `.claude/hooks/`、
判定のテストは `tests/test_hooks.py`。

## 未検証のもの

- **8/26に入れた関連動画16本の効果はまだ測れていない。** Analytics は2〜3日遅れる。
  `RELATED_VIDEO` は今日時点で0
- **終了画面・カード・透かし・再生リストが効くかは、置いていないので誰も知らない**
- **予約公開で通知が飛んでいない件**は、通知列が全行「—」であることまでしか確認していない。
  「通知を送れば伸びる」かどうかは未検証（登録4人なので今は誤差）
- **関連動画の候補が0件になる原因は不明。** 通信は200で返っている。
  「新しく公開した動画は候補に出ない」のか「候補の仕組みが変わった」のかは分けられていない
- 08-20 の配信断絶の原因は不明のまま
- 全動画に `suggestions.processingHints: ["nonStreamableMov"]`。実害は確かめていない
- 今日のクォータ消費は測っていない。Studio の操作は Data API を使わない。
  読み取りに使ったのは `report_stats.py` と `audit_account.py` で十数ユニット
