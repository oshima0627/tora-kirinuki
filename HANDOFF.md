# 引き継ぎ

最終更新: 2026-08-27

## いま何をしているのか

**Studio を調査して、埋まっていなかった導線を実際に埋めた。**

配線を妨げる設定は今回も1つも無かった。問題は設定ではなく、
**視聴者を次に運ぶ部品が置かれていない**こと。今回そのうち
**透かし・再生リスト**を片付け、**カード**を7本、**終了画面**を2本に入れた。

調査の全体は `docs/2026-08-27-studio-audit.md`（誤りは同ファイル内で訂正済み）。

数字は動いていない。**長尺は全期間で視聴28・インプレッション366、登録者は4人。**
今日入れたものが効くかどうかは、これから測る。

## 今回やったこと

| ファイル | 何を |
|---|---|
| `docs/2026-08-27-studio-audit.md` | 新規。Studio 全画面の実測。終了画面の記述はあとで訂正した |
| `scripts/sync_playlists.py` | 新規。再生リストをレシピの判定カードに合わせて揃える。既定はドライラン |
| `tests/test_sync_playlists.py` | 新規。分類関数のテスト4本 |
| `scripts/build_brand.py` | `build_watermark()` を追加（150x150・透明地）。`--guide` なしでも出る |

実行したコマンド:
`python scripts/report_stats.py --traffic` / `python scripts/audit_account.py` /
`python scripts/build_brand.py` / `python scripts/sync_playlists.py [--apply]` /
`python -m pytest tests/test_sync_playlists.py -q` → **4 passed**

## 検証済みの事実（実際に画面に出したもの）

### 入れたもの

- **透かし（再生中の登録ボタン）を設定した。** 150x150の透明PNG（赤い虎）。
  表示タイミングは **「動画全体」**（「動画の最後」では平均視聴3:01の視聴者に届かない）。
  カスタマイズ画面を再読み込みしても残っており、
  **`youtube.com/watch?v=xlqEPCrF6cI` のプレーヤー右下に実際に出ているのを目で確認した**
- **再生リストを3本に整理した。** `sync_playlists.py --apply` の実行ログと、
  実行後の再取得で確認:

  | ID | 本数 | 名前 |
  |---|---|---|
  | `PLJ2GVKaPKNKI` | 18本 | 完全ALL成立回｜令和の虎Second 切り抜き |
  | `PLLKRI2G5rY1o` | 8本 | 不成立・虎が詰めた回｜令和の虎Second 切り抜き |
  | `PLYr3WAVLad7E` | 6本 | 協力確約で終わった回｜令和の虎Second 切り抜き（**新規**） |

  - **前は「完全ALL成立回」12本のうち6本が台帳外の非公開動画だった。** 視聴者には
    「非公開の動画」としか出ない。その6本を外し、公開分24本を足した
  - もう一度ドライランを回すと **外す0・足す0・0ユニット**（べき等）
- **カードを長尺7本に入れた**（再生リストへのリンク・0:45起点・ティーザーテキスト付き）:

  | 動画 | 指す再生リスト |
  |---|---|
  | `xlqEPCrF6cI` `OsHhgXI5OgI` `aPof751FH1U` `id6KrvcOzy0` | 完全ALL成立回 |
  | `phatA361FTg` `VwO47R4gGsE` `axZrTqutD34` | 不成立・虎が詰めた回 |

  `xlqEPCrF6cI` は再読み込みして「0:45:00 / 完全ALL成立回 / ティーザー入り」が
  残っていることを確認した
- **終了画面を2本に入れた**（チャンネル登録＋「視聴者に適したコンテンツ」）:
  `O3_81hOC0j4` `aPof751FH1U`。`O3_81hOC0j4` は再読み込みして残存を確認

### 分かったこと

- **「終了画面ゼロ」は誤りだった。** `VwO47R4gGsE`（8/13）には最初から入っていた。
  最初に見た `aPof751FH1U` 1本からの一般化だった。**全数はまだ数えていない**
- **「特定の動画の選択」（関連動画・動画カード）は今日は何を検索しても0件を返す。**
  `aPof751FH1U` / `O3_81hOC0j4` / `Pi1SplRFskQ` / 「令和の虎」いずれも一致なし。
  `POST /youtubei/v1/creator/list_creator_videos` は **200** を返しているので通信の失敗ではない
- **「特定の再生リストの選択」は正常に動く。** 検索せずに一覧が出る。
  動画の選択だけが壊れている
- **Studio 上部の「187」は vidIQ 拡張の数字。登録者数ではない**（実際は4人）
- **ブラウザの既定チャンネルは別チャンネル**（「日本の最新ニュースまるわかり」）。
  `studio.youtube.com/` を素で開くとそちらが出る。必ず
  `studio.youtube.com/channel/UCWupHXqf8CTG00c_eoQSw1Q` から入る
- **API のクォータは JST 15時台にはもう尽きていた。** `playlists.insert` が 403
  `quotaExceeded`。**PT日替わり＝JST 16:00 に回復**し、16:02 の再実行は全件通った

### 数字（今日の実測）

- 過去28日: 視聴 2.9万 / 総再生 186.6時間 / 登録 +4。**内訳はショート99.9%・動画0.1%**
- 全期間・「動画」フィルタ: 視聴 **28** / インプレッション **366** / CTR 4.9% / 平均視聴時間 3:01
- トラフィック: `SHORTS 25217` `YT_SEARCH 402` `YT_OTHER_PAGE 264` `YT_CHANNEL 124`
  `EXT_URL 6` `SUBSCRIBER 4`。**`RELATED_VIDEO` は0**
- 収益化（8/21時点）: 有効な総再生時間 **0時間**/3,000、有効ショート視聴 5,436/300万、登録 4/500
- **台帳に無い private が10本**。うち `yHxbK4Y05zI` は `NSAi-BfomiU`（8/28予約）の**重複アップロード**

## 次にやること

### 1. 8/28・8/29 の公開分を流し込む（公開後に）

予約が明けたら、その日のうちに:

```bash
cd C:/Users/oshim/Documents/projects/tora-kirinuki
python scripts/sync_playlists.py          # 差分を見る
python scripts/sync_playlists.py --apply  # 入れる
```

`komazawa-ginkou`（成立）と `yotsui-kakkoii`（不成立）の各2本が入る。
**クォータは JST 16:00 に回復する。** 午後に回すなら16時以降に。

### 2. 関連動画をもう一度試す

今日は候補が0件で入れられなかった。時間をおいて開き直す:

```
studio.youtube.com/video/U9FMPUTeZ-Y/edit
→ 右カラム「関連動画」の鉛筆 → 検索欄に aPof751FH1U
→ 出た1件をクリック → 右上「保存」（グレーになれば保存済み）
```

| ショート | 長尺 |
|---|---|
| `U9FMPUTeZ-Y` | `aPof751FH1U` |
| `ji7bvZ1syLA` | `NSAi-BfomiU`（8/28公開） |
| `Yl3wV9uQnxU` | `B9iCtgqAZaY`（8/29公開） |

### 3. カードを残りの長尺にも入れる

APIには無いので Studio で1本ずつ。入っていないのは9本:

| 指す先 | 動画 |
|---|---|
| 完全ALL成立回 | `EDRPTR5yGUs` `VBYY-SU03uM` `9ngQ_04cv0k` `K_eAlE4vS6o` `_COv48LWvT0` |
| 不成立 | `RSyPpzXCUiI` |
| 協力確約 | `QFKFzKi3zuo` `3Be5bKiduVY` `O3_81hOC0j4` |

手順（1本あたり）:

```
studio.youtube.com/video/<ID>/edit
→ 右カラム「カード」の鉛筆（位置は動画ごとに違う。押す前に画面を見る）
→ 「再生リスト」の＋ → 一覧から選ぶ（検索は要らない）
→ 時刻を 0:45 に → ティーザーテキストを入れる → 右上「保存」
```

**＋を2回押すとカードが2枚できる。** 1回押して10秒待つ。
2枚できたらゴミ箱で消してから続ける（今日2回やらかした）。

### 4. 効果を測る

入れたのは 8/27。Analytics は2〜3日遅れるので **8/30以降**に見る。

```bash
python scripts/report_stats.py --traffic
```

見るのは `RELATED_VIDEO` と `YT_PLAYLIST` が0から動くか。
Studio 側では アナリティクス > コンテンツ > **「動画」フィルタ**で
`cardImpressions`（カードのインプレッション）と終了画面のクリック。
**「すべて」フィルタと取り違えないこと**（§5の食い違いの原因かもしれない）。

### 5. まだ埋めていない穴

- 前回の「インプレッション 2,387」と今日の「366」が**6.5倍食い違っている**。
  次に見るときはフィルタを画面で確かめてから記録する
- **終了画面が入っている長尺は全数を数えていない。** 入っているのが分かっているのは
  `VwO47R4gGsE` `O3_81hOC0j4` `aPof751FH1U` の3本
- 重複アップロード `yHxbK4Y05zI` をどうするか。**削除は取り返しがつかないので相談してから**
- 長尺の残存カーブ（`elapsedVideoTimeRatio`）は未測定

## 触ってはいけないところ・保留中の判断

**Studio の保存はダイアログが閉じきる前に押しても効かない。**
**「保存」がグレーになったことを必ず目で確認する。**

**カード編集で「＋」や再生リストのタイルを2回押すと2枚できる。**
1回押して待つ。できてしまったらゴミ箱で消す。

**`upload_youtube.py` はカレントディレクトリで台帳の場所が変わる。**
`ROOT = Path(__file__).parents[1]` なので、ワークツリーから実行するとワークツリー側の
`state/published.json` を見る。**実行は本体（`C:/Users/oshim/Documents/projects/tora-kirinuki`）
からに統一する。** `yHxbK4Y05zI` はこれで二重に上がった可能性が高い。

**`sync_playlists.py` は非公開の動画を入れない。** 予約公開の分は公開日に流し直す。
判定が「成立／不成立／協力確約」のどれにも当たらない回（`mai-teian` の
「この時点で100万円」）は**触らない**。推測で分類しない。

**音を聞かないと確定しないASRの崩れが残っている。推測で `recipe.fixes` に足さないこと。**

| レシピ | 残っている崩れ |
| --- | --- |
| `akutsu-hantei` | 「めっちゃおろいや」「え、ごめんなさい。しょが」 |
| `imamura-ai` | 「ただ1点研したサービス」「今姉社長が」「いやいやいですか?」 |
| `komazawa-ginkou` | 「価確」（2箇所）「知持っております」「私もせていただきます」「津田さん」「紫さん」 |
| `yotsui-kakkoii` | 「リアルバリオー」「会社員さんみたいに」 |

`python scripts/build_short.py recipes/<id>.json --dry-run` で焼く字幕が全部出る。

**8/26公開の `Atsm_3CKLx8`（akutsu-hantei-short）は焼き込み字幕が訂正前のまま。**
差し替えるなら公開済み動画の取り下げになるので、やるかどうかは相談してから。

**8/30以降の公開枠は空**（新しく作る素材が無い）。予約は 8/29 まで。

**引き継ぎはフックで強制されている。** `SessionStart` でこのファイルが context に流し込まれ、
`Stop` で未pushかつ `HANDOFF.md` 未更新なら止まる。中身は `.claude/hooks/`、
判定のテストは `tests/test_hooks.py`。

## 未検証のもの

- **今日入れたもの（透かし・再生リスト・カード・終了画面）が数字を動かすかは分からない。**
  入れたのが 8/27。効果を見るのは 8/30 以降
- **カード7本のうち再読み込みで確認したのは `xlqEPCrF6cI` の1本だけ。**
  残り6本は保存ダイアログが閉じたことしか見ていない
- **終了画面が実際にプレーヤーに出ているかは見ていない**（透かしは見た）
- **関連動画の候補が0件になる原因は不明。** 通信は200で返っている
- **8/26に入れた関連動画16本の効果もまだ測れていない。** `RELATED_VIDEO` は今日時点で0
- 08-20 の配信断絶の原因は不明のまま
- 全動画に `suggestions.processingHints: ["nonStreamableMov"]`。実害は確かめていない
- **今日のクォータは15時台に尽きていた。** 何が使ったかは測っていない。
  今日 16:02 以降に使ったのは playlists/playlistItems の書き込み **1,550ユニット**と
  読み取り数十ユニット
